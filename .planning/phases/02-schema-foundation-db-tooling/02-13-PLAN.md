---
phase: 02-schema-foundation-db-tooling
plan: 13
type: tdd
wave: 9
depends_on: []
files_modified:
  - packages/core/tests/setup/container.ts
autonomous: true
gap_closure: true
requirements: [FND-12]

must_haves:
  truths:
    - "PG_IMAGE in container.ts references the image by digest (contains '@sha256:')"
    - "D-40 reproducibility requirement is satisfied — CI and local machines use identical PG image bytes"
    - "The grep gate for digest presence passes"
  artifacts:
    - path: "packages/core/tests/setup/container.ts"
      provides: "Single source of truth for PG_IMAGE — now pinned by digest per D-40"
      contains: "@sha256:"
  key_links:
    - from: "packages/core/tests/setup/container.ts"
      to: "testcontainers PG container"
      via: "PG_IMAGE constant consumed by globalSetup"
      pattern: "PG_IMAGE.*@sha256:"
---

<objective>
Pin the PG_IMAGE constant in `packages/core/tests/setup/container.ts` from a floating tag (`postgres:18-alpine`) to a digest-pinned reference (`postgres:18-alpine@sha256:<digest>`), satisfying D-40 (CONTEXT.md).

Purpose: Floating tags are non-reproducible across time and machines. A digest pin guarantees that CI and every developer run the exact same image bytes, which is the requirement D-40 mandated. The comment in container.ts already documents the intent — this plan executes it.

Output:
- `PG_IMAGE` value updated to include `@sha256:<digest>` suffix.
- The digest is fetched from the running Docker daemon via `docker pull` + `docker inspect`.
- A grep-based acceptance test confirms the digest is present.
</objective>

<execution_context>
@/Users/jvescudero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jvescudero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jvescudero/projects/apruma/.planning/PROJECT.md
@/Users/jvescudero/projects/apruma/.planning/ROADMAP.md
@/Users/jvescudero/projects/apruma/.planning/STATE.md
</context>

<interfaces>
<!-- Existing file content: packages/core/tests/setup/container.ts (current, gap) -->
```typescript
// Single source of truth for the PG image used in testcontainers.
// Per D-40: pinned to postgres:18-alpine for reproducibility.
// To pin by digest: 'postgres:18-alpine@sha256:<hash>'
// Get digest: docker pull postgres:18-alpine && docker inspect postgres:18-alpine | jq '.[0].RepoDigests'
export const PG_IMAGE = "postgres:18-alpine";
```

<!-- Target form after fix -->
```typescript
// Per D-40: pinned by digest for full reproducibility (tag alone is non-deterministic).
// Re-pin: docker pull postgres:18-alpine && docker inspect --format='{{index .RepoDigests 0}}' postgres:18-alpine
export const PG_IMAGE = "postgres:18-alpine@sha256:<actual_digest>";
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing grep test asserting digest presence</name>
  <files>packages/core/tests/setup/container.ts</files>

  <read_first>
    - packages/core/tests/setup/container.ts — current content (PG_IMAGE value, D-40 comment)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md section D-40 — exact requirement (digest pin, single constant, CI + local reproducibility)
  </read_first>

  <behavior>
    - Verification assertion (grep gate): `grep -E '@sha256:[a-f0-9]{64}' packages/core/tests/setup/container.ts` returns exit code 0 (match found).
    - The current file has `PG_IMAGE = "postgres:18-alpine"` (no digest) — the grep exits non-zero (RED).
    - After the fix (Task 2), the same grep exits 0 (GREEN).
  </behavior>

  <action>
    Confirm RED state by running the grep command against the current file. Do not modify the file. Record the exit code (must be non-zero).

    This is a structural assertion task, not a code creation task. The "test" is the grep gate itself — a deterministic, automated check that verifies the contract expressed in D-40.

    Command to confirm RED:
    `grep -E '@sha256:[a-f0-9]{64}' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts`
    Expected: exit 1 (no match — RED confirmed).
  </action>

  <verify>
    <automated>grep -E '@sha256:[a-f0-9]{64}' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts; echo "exit:$?"</automated>
  </verify>

  <acceptance_criteria>
    - Grep command exits non-zero against the current file (RED confirmed).
    - File is NOT modified in this task.
  </acceptance_criteria>

  <done>
    RED confirmed: current container.ts does not contain a digest-pinned PG_IMAGE. Grep exits 1.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Fetch digest and pin PG_IMAGE</name>
  <files>packages/core/tests/setup/container.ts</files>

  <read_first>
    - packages/core/tests/setup/container.ts — full current content (comments document the exact docker command to use)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md D-40 — digest requirement
  </read_first>

  <behavior>
    - After this task, `PG_IMAGE` equals `"postgres:18-alpine@sha256:<64-hex-char-digest>"`.
    - The digest is fetched live from Docker (not invented or hardcoded from memory).
    - The comment block is updated to remove the "To pin:" instruction (already done) and retain the re-pin recipe.
    - `grep -E '@sha256:[a-f0-9]{64}' packages/core/tests/setup/container.ts` exits 0 (GREEN).
    - Testcontainers still boots with the new PG_IMAGE value — no runtime behavior change, only the image ref becomes deterministic.
  </behavior>

  <action>
    Step 1 — Fetch the digest for postgres:18-alpine:
    Run `docker pull postgres:18-alpine` to ensure the image is present and up to date.
    Run `docker inspect --format='{{index .RepoDigests 0}}' postgres:18-alpine` to obtain the full digest string.
    The output will be of the form `postgres@sha256:<64-hex>` or `docker.io/library/postgres@sha256:<64-hex>`.
    Extract the sha256 hex value (64 lowercase hex characters).

    Step 2 — Update container.ts:
    Replace the current `PG_IMAGE` string value with `"postgres:18-alpine@sha256:<extracted_digest>"`.
    Update the comments:
    - Remove the "To pin by digest:" instruction line (it is now done).
    - Keep the re-pin recipe comment so future maintainers know how to update it.
    - Update the "Per D-40" comment to read: "Per D-40: pinned by digest for full reproducibility."

    Step 3 — Verify GREEN:
    `grep -E '@sha256:[a-f0-9]{64}' packages/core/tests/setup/container.ts`
    Must exit 0.

    Step 4 — Run test suite smoke check:
    `pnpm --filter @aprumo/core test --reporter=dot 2>&1 | tail -5`
    Confirm testcontainers still starts (no "Cannot pull image" or "image not found" errors).

    Note: if Docker is not running in the execution environment, use the fallback approach — document the exact commands the developer must run to pin the digest, and set PG_IMAGE to the most recently published sha256 for postgres:18-alpine from Docker Hub (fetch via `docker pull` output or `docker manifest inspect postgres:18-alpine`). The file must end up with a real sha256 digest, not a placeholder.
  </action>

  <verify>
    <automated>grep -E '@sha256:[a-f0-9]{64}' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts && echo "PASS: digest present"</automated>
    <automated>grep -v '^//' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts | grep -c '"postgres:18-alpine"'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E '@sha256:[a-f0-9]{64}' packages/core/tests/setup/container.ts` exits 0 (GREEN).
    - `grep -v '^//' packages/core/tests/setup/container.ts | grep -c '"postgres:18-alpine"'` returns 0 — no non-comment lines contain the old floating-tag-only value.
    - The digest in the file is exactly 64 lowercase hex characters after `@sha256:`.
    - `pnpm typecheck` exits 0 (container.ts is TypeScript, must remain type-valid).
    - `pnpm lint` exits 0.
  </acceptance_criteria>

  <done>
    PG_IMAGE is digest-pinned. D-40 is fully satisfied. The grep gate passes. Testcontainers behavior is unchanged — only the image reference is now deterministic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker pull → container.ts | Digest sourced from Docker Hub via local docker daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages; container.ts is a single-constant TypeScript file |
| T-02-13-01 | Tampering | PG_IMAGE digest value | accept | Digest is a SHA-256 content hash — tampering with the image would change the digest, causing Docker pull to fail with a verification error; this is the exact security property digest pinning provides |
</threat_model>

<verification>
After Task 2 completes:

1. Digest present:
   `grep -E '@sha256:[a-f0-9]{64}' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts`
   Expected: exit 0, prints the PG_IMAGE line.

2. No floating tag residue:
   `grep -v '^//' /Users/jvescudero/projects/apruma/packages/core/tests/setup/container.ts | grep -c '"postgres:18-alpine"'`
   Expected: 0.

3. TypeScript valid:
   `pnpm typecheck`
   Expected: exit 0.

4. Lint clean:
   `pnpm lint`
   Expected: exit 0.
</verification>

<success_criteria>
- `PG_IMAGE` in `packages/core/tests/setup/container.ts` contains `@sha256:` followed by a 64-character lowercase hex digest.
- D-40 fully satisfied: digest matches a real postgres:18-alpine image digest from Docker Hub.
- No floating-tag reference remains in the non-comment portion of the file.
- `pnpm typecheck` and `pnpm lint` exit 0.
</success_criteria>

<output>
Create `.planning/phases/02-schema-foundation-db-tooling/02-13-SUMMARY.md` when done.
</output>
