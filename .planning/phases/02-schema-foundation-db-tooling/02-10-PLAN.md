---
phase: "02-schema-foundation-db-tooling"
plan: "10"
type: execute
wave: 8
depends_on:
  - "02-07"
  - "02-08"
  - "02-09"
files_modified:
  - packages/core/tests/e2e/migration-e2e.integration.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FND-11
  - FND-13
  - FND-16

must_haves:
  truths:
    - "[BLOCKING] pnpm db:migrate runs to completion on a fresh postgres:18-alpine container producing all ledger tables"
    - "CI integration test after migration asserts pg_constraint condeferrable+condeferred=true (FND-11)"
    - "CI integration test after migration asserts aprumo_app UPDATE postings returns 42501 (FND-08)"
    - "CI integration test after migration calls post_transaction with balanced postings and returns UUID"
    - "pnpm db:reset destroys and recreates DB; pnpm db:migrate reapplies all migrations"
    - "GitHub Actions has integration-test job that starts postgres, runs db:migrate, runs e2e test suite"
  artifacts:
    - path: "packages/core/tests/e2e/migration-e2e.integration.test.ts"
      provides: "End-to-end migration verification: all migrations applied, schema correct, function works"
    - path: ".github/workflows/ci.yml"
      provides: "integration-test job with postgres service + db:migrate + test run"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/core/tests/e2e/migration-e2e.integration.test.ts"
      via: "pnpm db:migrate && pnpm test"
      pattern: "db:migrate"
    - from: "packages/core/tests/e2e/migration-e2e.integration.test.ts"
      to: "packages/core/migrations/"
      via: "createTestDb applies all migrations"
      pattern: "createTestDb"
---

<objective>
[BLOCKING] The phase goal task: verify pnpm db:migrate runs end-to-end on a fresh Postgres container, add the comprehensive CI integration test that asserts the full migration stack is correct, and wire the CI integration-test job. This is Success Criterion 1 of Phase 2.

Purpose: TypeScript compilation is necessary but not sufficient. We must prove that all migrations apply correctly against a real PG container, producing the correct schema, function, trigger, and permissions. This is the phase gate.

Output: migration-e2e integration test, updated ci.yml with integration-test job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md
@.planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md
@.planning/phases/02-schema-foundation-db-tooling/02-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive migration E2E integration test</name>
  <files>packages/core/tests/e2e/migration-e2e.integration.test.ts</files>
  <read_first>
    - .planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md §Code Examples (all three integration test patterns: FND-08, FND-11, FND-09+FND-10)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md §D-37 (createTestDb pattern), §D-39 (app vs migration pools)
    - ROADMAP.md §Phase 2 Success Criteria (all 6 criteria)
    - REQUIREMENTS.md §FND-11, FND-13, FND-16
  </read_first>
  <action>
    Create `packages/core/tests/e2e/migration-e2e.integration.test.ts`.
    This is the comprehensive "all migrations applied" test. Use `createTestDb(import.meta.url)`.

    Write the following test suite (each a separate `it()` block):

    Test group "All tables exist after migration":
    - Query `information_schema.tables WHERE table_schema = current_schema()` and assert all 10 tables exist: `accounts`, `transactions`, `postings`, `raw_events`, `account_balance`, `outbound_endpoints`, `outbound_events`, `accounts_audit`, `outbound_endpoints_audit`, `outbound_events_audit`

    Test group "FND-08: REVOKE enforcement" (inline, so this test is self-contained):
    - `db.app.query('UPDATE postings SET amount_cents = 0 WHERE id = gen_random_uuid()')` → rejects with `{ code: '42501' }`
    - `db.app.query('DELETE FROM raw_events WHERE id = gen_random_uuid()')` → rejects with `{ code: '42501' }`

    Test group "FND-11: assert_double_entry trigger is deferrable":
    - Query pg_constraint WHERE conname='assert_double_entry'; assert condeferrable=true AND condeferred=true
    - Assert the row is defined (trigger exists)

    Test group "FND-09: post_transaction works end-to-end":
    - BeforeAll: insert 2 accounts via migration pool
    - Call `post_transaction(uuid, 'e2e test', 'test', '{}', ARRAY[ROW(acc1, 100, 'debit'), ROW(acc2, 100, 'credit')])` via db.app
    - Assert returned UUID matches UUID regex
    - Query `postings WHERE transaction_id = $1` via migration pool; assert 2 rows with correct amounts

    Test group "FND-09: post_transaction rejects unbalanced postings":
    - Call with sum ≠ 0; assert rejects with `{ code: 'P0001' }`

    Test group "FND-11: post_transaction idempotency":
    - Call twice with same idempotency_key; assert both resolve; assert same UUID returned both times

    BeforeAll pattern: `db = await createTestDb(import.meta.url)` (inject() called inside createTestDb).
    AfterAll: `await db.cleanup()`.

    Commit as: `test(02-10): add migration E2E integration test suite`.
  </action>
  <verify>
    <automated>
      pnpm --filter @aprumo/core test --run packages/core/tests/e2e/migration-e2e.integration.test.ts 2>&amp;1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists with 6 test groups covering tables, REVOKE, pg_constraint, post_transaction success, post_transaction failure, idempotency
    - Uses createTestDb with proper beforeAll/afterAll cleanup
    - All tests pass green when run against testcontainers (all prior plans complete)
    - `pnpm --filter @aprumo/core test --run` passes for this file
  </acceptance_criteria>
  <done>Comprehensive E2E migration test file committed and passing green against testcontainers PG.</done>
</task>

<task type="auto">
  <name>Task 2: Wire integration-test CI job with db:migrate verification</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-ci-dev-security/01-CONTEXT.md §D-12 (CI workflow shape: composite setup action, parallel jobs)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md §D-40 (postgres:18-alpine image)
    - .github/workflows/ci.yml (existing CI — add integration-test job; do not break existing jobs)
    - ROADMAP.md §Phase 2 Success Criteria 1 (pnpm db:migrate runs to completion)
    - REQUIREMENTS.md §FND-11, FND-13, FND-16
  </read_first>
  <action>
    Update `.github/workflows/ci.yml` to add an `integration-test` job.

    The job requires Docker (testcontainers needs Docker daemon). GitHub Actions `ubuntu-latest` runners have Docker preinstalled.

    Job structure:
    ```yaml
    integration-test:
      needs: [lint, typecheck, build]  # run after basic checks pass
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: ./.github/actions/setup  # reuse Phase 1 composite action
        - name: Check migration drift
          run: node scripts/check-migration-drift.mjs
        - name: Run integration tests (testcontainers)
          run: pnpm --filter @aprumo/core test --run
          env:
            DATABASE_URL: ''  # not needed — testcontainers manages its own container
            TESTCONTAINERS_RYUK_DISABLED: 'true'  # CI environments may not support Ryuk
    ```

    Important CI notes for testcontainers on GitHub Actions:
    - Set `TESTCONTAINERS_RYUK_DISABLED: 'true'` — Ryuk (testcontainers cleanup daemon) can fail on GitHub-hosted runners
    - Docker daemon is available on ubuntu-latest (no additional setup needed)
    - `DOCKER_HOST` is set automatically

    Also add `integration-test` to the branch protection required status checks list (comment in ci.yml explaining this — actual branch protection is configured in GitHub UI).

    Verify the existing jobs (lint, typecheck, test, coverage-gate, build, changeset-check, gitleaks-history) remain unchanged.

    Commit as: `ci(02-10): add integration-test job with testcontainers + migration drift check`.
  </action>
  <verify>
    <automated>
      grep -c "integration-test" .github/workflows/ci.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `integration-test` job exists in ci.yml with `runs-on: ubuntu-latest`
    - Job includes `node scripts/check-migration-drift.mjs` step (FND-15)
    - Job includes `pnpm --filter @aprumo/core test --run` with TESTCONTAINERS_RYUK_DISABLED=true
    - Job uses existing composite `.github/actions/setup` action
    - Existing jobs (lint, typecheck, test, coverage-gate, etc.) are unchanged
    - `grep -c "integration-test" .github/workflows/ci.yml` returns ≥ 1
  </acceptance_criteria>
  <done>CI integration-test job added. pnpm db:migrate runs in testcontainer context. All Phase 2 Success Criteria verifiable in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Docker→testcontainer | CI runner must have Docker access; RYUK disabled for reliability |
| db:migrate→blank PG container | Migration must be idempotent on fresh container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Privilege Escalation | aprumo_app bypassing REVOKE | mitigate | E2E test asserts 42501 for UPDATE postings via aprumo_app pool; runs in every CI integration test run |
| T-2-02 | Tampering | schema drift in CI | mitigate | check-migration-drift.mjs runs as CI step before integration tests; exits 1 on any edited migration |
| T-2-03 | Tampering | unbalanced postings via post_transaction | mitigate | E2E test asserts P0001 for unbalanced postings via db.app; post_transaction is sole write path |
| T-2-04 | Information Disclosure | DATABASE_URL in CI logs | mitigate | testcontainers manages its own container; DATABASE_URL env var is empty in CI; pgUri from inject() is testcontainer-managed |
| T-2-05 | Tampering | test isolation in CI | mitigate | schema-per-file isolation; TESTCONTAINERS_RYUK_DISABLED=true for reliability |
| T-2-SC | Tampering | npm installs in CI | transfer | Completed Plan 01 checkpoint |
</threat_model>

<verification>
Phase 2 gate verification (run in order):
1. `node scripts/check-migration-drift.mjs` exits 0
2. `pnpm --filter @aprumo/core test --run 2>&1 | grep -E "PASS|FAIL"` — all integration tests green
3. `pnpm --filter @aprumo/core test --run 2>&1 | grep "migration-e2e"` shows this test file passing
4. `grep "condeferrable" packages/core/tests/e2e/migration-e2e.integration.test.ts` matches
5. `grep "42501" packages/core/tests/e2e/migration-e2e.integration.test.ts` matches
6. `grep "post_transaction" packages/core/tests/e2e/migration-e2e.integration.test.ts` matches
7. `grep "TESTCONTAINERS_RYUK_DISABLED" .github/workflows/ci.yml` matches
</verification>

<success_criteria>
- [BLOCKING] pnpm db:migrate applies all 6 migrations (0000–0005) on fresh testcontainers PG
- E2E integration test passes: all 10 tables exist, REVOKE enforced (42501), trigger deferrable, post_transaction works
- CI integration-test job added with testcontainers support
- All Phase 2 success criteria (ROADMAP.md) are machine-verifiable in CI
</success_criteria>

<output>
Create `.planning/phases/02-schema-foundation-db-tooling/02-10-SUMMARY.md` when done
</output>
