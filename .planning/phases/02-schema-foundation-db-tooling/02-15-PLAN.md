---
phase: 02-schema-foundation-db-tooling
plan: 15
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/db/seed.ts
  - packages/core/tests/infra/seed-guard.test.ts
autonomous: true
requirements: [FND-14]
gap_closure: true
gap_source: 02-UAT.md
gap_tests: [4]

must_haves:
  truths:
    - "pnpm --filter @aprumo/core db:seed exits 0 against a live DB"
    - "After db:seed, SELECT COUNT(*) FROM accounts >= 2, FROM transactions >= 1, FROM postings >= 2"
    - "The WR-06 guard still rejects a seed file whose meaningful content does not begin with DO $$"
    - "Migration drift check (node scripts/check-migration-drift.mjs) still exits 0"
    - "Full test suite (pnpm --filter @aprumo/core test) still exits 0"
  artifacts:
    - path: packages/core/src/db/seed.ts
      provides: "assertSeedStructure() pure function + updated runSeed() that calls it"
      contains: "assertSeedStructure"
    - path: packages/core/tests/infra/seed-guard.test.ts
      provides: "Unit tests for assertSeedStructure() — RED before fix, GREEN after"
      exports: []
  key_links:
    - from: packages/core/tests/infra/seed-guard.test.ts
      to: packages/core/src/db/seed.ts
      via: "import { assertSeedStructure }"
      pattern: "assertSeedStructure"
---

<objective>
Close UAT gap: pnpm db:seed always exits 1 because the WR-06 structural-validation
guard in seed.ts rejects 0006_seed_dev.sql, which legitimately opens with 8 lines of
SQL line-comments before its DO $$ block.

Purpose: Restore the db:seed command to working order without weakening the WR-06
security guarantee. The guard must continue to reject any seed file whose non-comment
content does not begin with DO $$.

Output:
- assertSeedStructure(content: string): void — pure, exported function that strips
  leading SQL line-comments and blank lines, then verifies the remaining content starts
  with DO $$. Throws on failure.
- seed-guard.test.ts — unit tests (no DB required) covering the fix and the security
  invariant.
- runSeed() updated to call assertSeedStructure() instead of the inline regex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/db/seed.ts
@packages/core/migrations/0006_seed_dev.sql
@packages/core/tests/infra/migration-drift.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing unit tests for assertSeedStructure()</name>
  <files>packages/core/tests/infra/seed-guard.test.ts</files>
  <behavior>
    The test file imports assertSeedStructure from packages/core/src/db/seed.ts.
    At this point assertSeedStructure does not exist, so every test in this file
    MUST fail (import error or assertion error). Commit with message
    "test(02-15): add failing tests for WR-06 assertSeedStructure guard".

    Required test cases (describe "assertSeedStructure"):

    ACCEPTANCE cases (must NOT throw):
    - "accepts DO $$ with no leading content" — input: "DO $$\nBEGIN\nEND $$;"
    - "accepts DO $$ preceded by leading whitespace only" — input: "  \n\nDO $$\nBEGIN\nEND $$;"
    - "accepts DO $$ preceded by SQL line-comments and blank lines" — input: the
      exact shape of 0006_seed_dev.sql: 8 comment lines ("-- DEV SEED ONLY...",
      "-- Run via: pnpm db:seed", etc.) followed by a blank line, then "DO $$\nBEGIN\nEND $$;"
    - "accepts DO $$ when inline comment follows on same line" — input:
      "-- header\n\nDO $$ -- optional comment\nBEGIN\nEND $$;"

    REJECTION cases (must throw Error matching /does not begin with.*DO \$\$/i):
    - "rejects empty string"
    - "rejects whitespace-only string"
    - "rejects file starting with INSERT INTO"
    - "rejects file starting with SELECT 1"
    - "rejects file where DO $$ appears only inside a comment (-- DO $$)"
    - "rejects file whose first real statement is CREATE TABLE, not DO $$"
  </behavior>
  <action>
    Create packages/core/tests/infra/seed-guard.test.ts. Import only:
      import { assertSeedStructure } from "../../../src/db/seed.js";
    Use vitest (describe, it, expect). No DB, no testcontainers, no beforeAll.

    The import will fail (assertSeedStructure is not exported yet) — that is the
    expected RED state. Run the test to confirm failure, then commit.

    File structure mirrors migration-drift.test.ts: ESM, __dirname via
    fileURLToPath(import.meta.url) only if path resolution is needed (it is not
    needed here — pure logic tests). Keep it simple.

    Do NOT add fenced code blocks in this action. Describe test inputs as prose.
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- --reporter=verbose packages/core/tests/infra/seed-guard.test.ts 2>&1 | grep -E "(FAIL|Cannot find|assertSeedStructure|SyntaxError)" | head -5</automated>
  </verify>
  <done>seed-guard.test.ts exists. Running the test file in isolation produces at least one
  failure (import error or assertion error). No test in this file passes yet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Extract assertSeedStructure() and fix the WR-06 guard</name>
  <files>packages/core/src/db/seed.ts</files>
  <behavior>
    After the fix:
    - assertSeedStructure("-- comment\n\nDO $$\nBEGIN\nEND $$;") does not throw
    - assertSeedStructure("INSERT INTO accounts ...") throws Error matching /does not begin with.*DO \$\$/i
    - All 10 test cases in seed-guard.test.ts pass
    - pnpm --filter @aprumo/core test exits 0 (full suite green)
    - pnpm --filter @aprumo/core db:seed exits 0 against a live DB
  </behavior>
  <action>
    Modify packages/core/src/db/seed.ts — two changes only:

    1. Extract and EXPORT a pure function assertSeedStructure(content: string): void
       placed ABOVE runSeed(). The algorithm:
         a. Split content into lines.
         b. Skip lines that are blank (after trim) or start with "--" (after trim).
         c. The first non-skipped line must match /^\s*DO\s+\$\$/.
         d. If no non-skipped line exists, OR the first non-skipped line does not
            match, throw: new Error(
              "Seed file at [path] does not begin with the expected DO $$ block. " +
              "Refusing to execute unrecognised content."
            )
            Because assertSeedStructure() does not know the file path, the error
            message should omit the path OR accept an optional label parameter —
            your choice. runSeed() can pass SEED_SQL_PATH as the label.

    2. Inside runSeed(), replace the inline regex guard (lines 43-48 of the
       current file) with a call to assertSeedStructure(seedContent).

    The function signature exported from seed.ts must be:
      export function assertSeedStructure(content: string, label?: string): void

    Keep the WR-06 audit comment above the call in runSeed() for traceability.
    Do not alter any other logic in seed.ts (postgres() connection, sql.unsafe(),
    isMain guard, process.exit calls).

    After implementing, run the test suite to confirm GREEN, then commit:
      "feat(02-15): extract assertSeedStructure — fix WR-06 guard for comment headers"

    Biome format will run via lefthook pre-commit. Ensure code uses double quotes,
    no trailing commas in function signatures (match existing file style), and
    process.stdout.write() (not console.log) for any output.
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    All of the following hold simultaneously:
    - pnpm --filter @aprumo/core test exits 0 (10 files including seed-guard.test.ts, 0 failures)
    - seed-guard.test.ts: all 10 cases pass (4 acceptance + 6 rejection)
    - pnpm --filter @aprumo/core db:seed exits 0 against DATABASE_URL with a live migrated DB
    - psql $DATABASE_URL -c "SELECT COUNT(*) FROM accounts" returns >= 2
    - psql $DATABASE_URL -c "SELECT COUNT(*) FROM transactions" returns >= 1
    - psql $DATABASE_URL -c "SELECT COUNT(*) FROM postings" returns >= 2
    - node scripts/check-migration-drift.mjs exits 0 (0006_seed_dev.sql untouched)
    - grep -c "export function assertSeedStructure" packages/core/src/db/seed.ts returns 1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → seed runner | seed.ts reads 0006_seed_dev.sql from disk and executes it as the migration role via sql.unsafe() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15-01 | Tampering | assertSeedStructure() in seed.ts | mitigate | The function strips only `--` line-comments and blank lines; any other leading content (INSERT, SELECT, CREATE) causes rejection. DO $$ structural check is preserved with the same intent as WR-06. |
| T-02-15-02 | Elevation of Privilege | sql.unsafe() executes seed as migration role | mitigate (existing) | assertSeedStructure() gate runs before sql.unsafe() call; unchanged by this fix. |
| T-02-15-SC | Tampering | npm/pnpm dependency install (none in this plan) | accept | No new package installs in this plan. |
</threat_model>

<verification>
Run these in order after both tasks are complete:

1. pnpm --filter @aprumo/core test -- --reporter=verbose
   Expected: all test files pass, 0 failures, seed-guard.test.ts present and green.

2. pnpm --filter @aprumo/core db:seed
   Expected: "Seed applied." printed to stdout, exit code 0.

3. psql $DATABASE_URL -c "SELECT COUNT(*) FROM accounts WHERE owner_ref='seed-owner'"
   Expected: >= 2

4. psql $DATABASE_URL -c "SELECT COUNT(*) FROM transactions"
   Expected: >= 1

5. psql $DATABASE_URL -c "SELECT COUNT(*) FROM postings"
   Expected: >= 2

6. node scripts/check-migration-drift.mjs
   Expected: "no drift detected (10 migrations verified)", exit 0.
</verification>

<success_criteria>
- seed-guard.test.ts exists with 10 test cases covering both acceptance and rejection paths
- assertSeedStructure is exported from packages/core/src/db/seed.ts
- The WR-06 inline regex guard in runSeed() is replaced by assertSeedStructure() call
- 0006_seed_dev.sql is NOT modified (migration immutability preserved, drift gate clean)
- pnpm --filter @aprumo/core test exits 0 (full suite including new unit tests)
- pnpm --filter @aprumo/core db:seed exits 0 against a migrated Postgres instance
- Post-seed row counts: accounts >= 2, transactions >= 1, postings >= 2
</success_criteria>

<output>
Create `.planning/phases/02-schema-foundation-db-tooling/02-15-SUMMARY.md` when done.
</output>
