---
phase: "02-schema-foundation-db-tooling"
plan: "03"
type: tdd
wave: 3
depends_on:
  - "02-02"
files_modified:
  - packages/core/migrations/0001_roles.sql
  - packages/core/migrations/0002_grants.sql
  - packages/core/migrations/meta/_journal.json
  - packages/core/tests/schema/revoke.integration.test.ts
autonomous: true
requirements:
  - FND-07
  - FND-08

must_haves:
  truths:
    - "aprumo_app role exists with NOLOGIN NOSUPERUSER"
    - "aprumo_migration role exists with NOLOGIN NOSUPERUSER CREATEDB"
    - "aprumo_app has SELECT and INSERT on all tables"
    - "aprumo_app cannot UPDATE or DELETE postings (SQLSTATE 42501)"
    - "aprumo_app cannot UPDATE or DELETE raw_events (SQLSTATE 42501)"
    - "ALTER DEFAULT PRIVILEGES ensures future tables inherit the grant pattern"
    - "Both migrations registered in _journal.json"
  artifacts:
    - path: "packages/core/migrations/0001_roles.sql"
      provides: "CREATE ROLE IF NOT EXISTS for aprumo_app and aprumo_migration"
      contains: "CREATE ROLE IF NOT EXISTS"
    - path: "packages/core/migrations/0002_grants.sql"
      provides: "GRANT SELECT/INSERT; REVOKE UPDATE/DELETE on postings,raw_events; DEFAULT PRIVILEGES"
      contains: "REVOKE UPDATE"
    - path: "packages/core/tests/schema/revoke.integration.test.ts"
      provides: "TDD: aprumo_app UPDATE postings expects 42501"
  key_links:
    - from: "packages/core/tests/schema/revoke.integration.test.ts"
      to: "packages/core/migrations/0002_grants.sql"
      via: "REVOKE UPDATE,DELETE ON postings,raw_events FROM aprumo_app"
      pattern: "42501"
---

<objective>
Write RED tests for REVOKE enforcement, then create 0001_roles.sql and 0002_grants.sql migrations to turn tests GREEN. Proves three-layer immutability at the role-permission layer.

Purpose: This is the first DB-level immutability layer. The test uses the aprumo_app pool to attempt UPDATE on postings — must receive SQLSTATE 42501. Tests must be RED before migrations exist.

Output: 0001_roles.sql, 0002_grants.sql, revoke integration test (GREEN after migrations).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md
@.planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 [RED]: Write REVOKE enforcement integration tests</name>
  <files>packages/core/tests/schema/revoke.integration.test.ts</files>
  <read_first>
    - .planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md §Code Examples "Integration Test: FND-08" (exact test pattern with createTestDb)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md §D-39 (app pool = aprumo_app role; migration pool = aprumo_migration role)
    - REQUIREMENTS.md §FND-08
  </read_first>
  <behavior>
    - Test "aprumo_app UPDATE postings → SQLSTATE 42501": `db.app.query('UPDATE postings SET amount_cents = 0 WHERE id = gen_random_uuid()')` rejects with `{ code: '42501' }`
    - Test "aprumo_app DELETE postings → SQLSTATE 42501": `db.app.query('DELETE FROM postings WHERE id = gen_random_uuid()')` rejects with `{ code: '42501' }`
    - Test "aprumo_app UPDATE raw_events → SQLSTATE 42501": `db.app.query('UPDATE raw_events SET status = \'failed\' WHERE id = gen_random_uuid()')` rejects with `{ code: '42501' }`
    - Test "aprumo_app DELETE raw_events → SQLSTATE 42501": `db.app.query('DELETE FROM raw_events WHERE id = gen_random_uuid()')` rejects with `{ code: '42501' }`
    - Test "aprumo_migration role has SELECT on accounts": `db.migration.query('SELECT COUNT(*) FROM accounts')` resolves without error
    RED: tests fail because 0001_roles.sql and 0002_grants.sql do not exist yet — REVOKE not applied.
  </behavior>
  <action>
    Create `packages/core/tests/schema/revoke.integration.test.ts`.
    Use the exact pattern from RESEARCH.md Code Examples §FND-08:
    Import `describe`, `it`, `beforeAll`, `afterAll`, `expect` from `vitest`.
    Import `createTestDb` and `TestDb` from `'../helpers/createTestDb.js'`.
    `let db: TestDb` — assign in `beforeAll(async () => { db = await createTestDb(import.meta.url) })`.
    `afterAll(async () => { await db.cleanup() })`.
    Write the 5 test cases from the behavior block above using `expect(...).rejects.toMatchObject({ code: '42501' })`.
    Commit as: `test(02-03): add RED REVOKE enforcement integration tests`.
    NOTE: Tests require Plan 08's createTestDb helper to run. They will be wired to run green together after Plan 08 and this plan complete.
  </action>
  <verify>
    <automated>MISSING — Wave 0: createTestDb helper (Plan 08) and 0001/0002 migrations required. Compile check: pnpm --filter @aprumo/core typecheck 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/tests/schema/revoke.integration.test.ts` exists
    - Tests assert `code: '42501'` for UPDATE and DELETE on postings/raw_events using `db.app`
    - Commit message starts with `test(02-03):`
  </acceptance_criteria>
  <done>RED test file committed.</done>
</task>

<task type="tdd">
  <name>Task 2 [GREEN]: Write 0001_roles.sql and 0002_grants.sql migrations</name>
  <files>
    packages/core/migrations/0001_roles.sql,
    packages/core/migrations/0002_grants.sql,
    packages/core/migrations/meta/_journal.json
  </files>
  <read_first>
    - .planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md §Pattern 4 (role permission model, exact SQL for 0001 and 0002, IF NOT EXISTS note, DEFAULT PRIVILEGES explanation)
    - .planning/phases/02-schema-foundation-db-tooling/02-RESEARCH.md §Pitfall 5 (CREATE ROLE IF NOT EXISTS)
    - .planning/phases/02-schema-foundation-db-tooling/02-CONTEXT.md §D-33 (drizzle-kit generate --custom to register hand-written migrations)
  </read_first>
  <behavior>
    After migrations apply: `db.app.query('UPDATE postings ...')` receives 42501. All 4 REVOKE tests pass green.
  </behavior>
  <action>
    Step 1 — Scaffold 0001_roles.sql via drizzle-kit (registers it in _journal.json):
    Run: `pnpm --filter @aprumo/core exec drizzle-kit generate --custom --name=roles`
    This creates an empty `migrations/0001_roles.sql` and appends entry to `_journal.json`.

    Step 2 — Write content of `packages/core/migrations/0001_roles.sql`:
    Use RESEARCH.md Pattern 4 exactly:
    `CREATE ROLE IF NOT EXISTS aprumo_app NOLOGIN NOSUPERUSER;`
    `CREATE ROLE IF NOT EXISTS aprumo_migration NOLOGIN NOSUPERUSER CREATEDB;`
    Add comment: `-- Roles are cluster-level; IF NOT EXISTS prevents failure on re-run after db:reset`
    Add comment: `-- Passwords set via docker-compose env vars or CI init script — never hardcoded here`
    No passwords in this file — per T-2-04 and CLAUDE.md invariants.

    Step 3 — Scaffold 0002_grants.sql:
    Run: `pnpm --filter @aprumo/core exec drizzle-kit generate --custom --name=grants`

    Step 4 — Write content of `packages/core/migrations/0002_grants.sql`:
    Use RESEARCH.md Pattern 4 exactly:
    `GRANT SELECT, INSERT ON ALL TABLES IN SCHEMA public TO aprumo_app;`
    `REVOKE UPDATE, DELETE ON TABLE postings FROM aprumo_app;`
    `REVOKE UPDATE, DELETE ON TABLE raw_events FROM aprumo_app;`
    `ALTER DEFAULT PRIVILEGES FOR ROLE aprumo_migration IN SCHEMA public GRANT SELECT, INSERT ON TABLES TO aprumo_app;`
    `GRANT USAGE ON ALL SEQUENCES IN SCHEMA public TO aprumo_app;`
    `ALTER DEFAULT PRIVILEGES FOR ROLE aprumo_migration IN SCHEMA public GRANT USAGE ON SEQUENCES TO aprumo_app;`
    `GRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA public TO aprumo_app;`
    `ALTER DEFAULT PRIVILEGES FOR ROLE aprumo_migration IN SCHEMA public GRANT EXECUTE ON FUNCTIONS TO aprumo_app;`

    Step 5 — Verify _journal.json now has entries for 0001_roles and 0002_grants.

    Commit as: `feat(02-03): add 0001_roles + 0002_grants migrations; revoke enforcement GREEN`.
  </action>
  <verify>
    <automated>
      grep -c "0002_grants" packages/core/migrations/meta/_journal.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `0001_roles.sql` uses `CREATE ROLE IF NOT EXISTS` for both roles (no passwords committed)
    - `0002_grants.sql` has REVOKE UPDATE, DELETE ON TABLE postings and raw_events FROM aprumo_app
    - `0002_grants.sql` has ALTER DEFAULT PRIVILEGES for future tables
    - `_journal.json` has entries for both 0001_roles and 0002_grants (grep returns ≥1)
    - No password literals in either .sql file
  </acceptance_criteria>
  <done>0001_roles.sql and 0002_grants.sql written and registered. When migrations run against testcontainers, REVOKE test turns green (42501 for UPDATE/DELETE on postings and raw_events from aprumo_app).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aprumo_app→postings | Role boundary: aprumo_app must not mutate postings or raw_events |
| migration file→git | Role creation SQL must never contain hardcoded passwords |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Privilege Escalation | aprumo_app→postings UPDATE/DELETE | mitigate | REVOKE UPDATE, DELETE ON TABLE postings/raw_events FROM aprumo_app in 0002_grants.sql; CI integration test asserts 42501 |
| T-2-02 | Tampering | schema drift | mitigate | 0001/0002 committed; Plan 09 drift check hashes both files |
| T-2-03 | Tampering | post_transaction bypass | accept (Plan 04) | REVOKE alone is not enough; SECURITY DEFINER in Plan 04 closes direct INSERT path |
| T-2-04 | Information Disclosure | role passwords in migration | mitigate | No passwords in 0001_roles.sql; comment explicitly says passwords via env vars only; gitleaks Phase 1 blocks any slip |
| T-2-05 | Tampering | test data leakage | accept (Plan 08) | createTestDb handles schema isolation |
| T-2-SC | Tampering | npm installs | transfer | Completed in Plan 01 checkpoint |
</threat_model>

<verification>
After plan completes:
1. `grep -c "IF NOT EXISTS" packages/core/migrations/0001_roles.sql` returns 2
2. `grep -c "REVOKE UPDATE" packages/core/migrations/0002_grants.sql` returns 2 (postings + raw_events)
3. `grep "PASSWORD\|password\|changeme" packages/core/migrations/0001_roles.sql` returns empty (no passwords)
4. `grep -c "DEFAULT PRIVILEGES" packages/core/migrations/0002_grants.sql` returns ≥ 2
5. REVOKE integration tests turn GREEN when run against testcontainers after Plan 08 + all migrations applied
</verification>

<success_criteria>
- Role creation migrations registered in _journal.json
- REVOKE on postings and raw_events applied to aprumo_app
- No passwords in committed migration files
- Integration tests (RED now) will turn GREEN when migrations run against real PG
</success_criteria>

<output>
Create `.planning/phases/02-schema-foundation-db-tooling/02-03-SUMMARY.md` when done
</output>
