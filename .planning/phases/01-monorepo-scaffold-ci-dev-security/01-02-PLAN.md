---
phase: "01-monorepo-scaffold-ci-dev-security"
plan: "02"
type: execute
wave: 1
depends_on: ["01-00", "01-01"]
files_modified:
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/tsconfig.build.json
  - packages/core/biome.json
  - packages/core/vitest.config.ts
  - packages/core/src/index.ts
  - packages/connector-base/package.json
  - packages/connector-base/tsconfig.json
  - packages/connector-base/tsconfig.build.json
  - packages/connector-base/biome.json
  - packages/connector-base/vitest.config.ts
  - packages/connector-base/src/index.ts
  - packages/connector-starkbank/package.json
  - packages/connector-starkbank/tsconfig.json
  - packages/connector-starkbank/tsconfig.build.json
  - packages/connector-starkbank/biome.json
  - packages/connector-starkbank/vitest.config.ts
  - packages/connector-starkbank/src/index.ts
  - packages/webhooks/package.json
  - packages/webhooks/tsconfig.json
  - packages/webhooks/tsconfig.build.json
  - packages/webhooks/biome.json
  - packages/webhooks/vitest.config.ts
  - packages/webhooks/src/index.ts
  - docs/adr/.gitkeep
autonomous: true
requirements:
  - INF-01
  - INF-04
  - INF-08

must_haves:
  truths:
    - "pnpm -r list --depth=0 resolves all 4 @aprumo/* package names"
    - "pnpm typecheck passes with no errors against stub src/index.ts files"
    - "pnpm build produces dist/ in each package via tsc project references"
    - "vitest run finds and runs tests in all 4 packages"
    - "Each package package.json exports field has development conditional pointing to src/"
  decisions:
    - D-07
    - D-08
    - D-09
    - D-10
    - D-19
    - D-21
    - D-30
  artifacts:
    - path: "packages/core/package.json"
      provides: "@aprumo/core package manifest with exports"
      contains: "@aprumo/core"
    - path: "packages/core/tsconfig.json"
      provides: "Per-package composite tsconfig extending base"
      contains: "composite"
    - path: "packages/core/vitest.config.ts"
      provides: "Per-package vitest project config with name and pool:forks"
      contains: "@aprumo/core"
    - path: "packages/core/src/index.ts"
      provides: "Stub placeholder export (empty export object)"
      contains: "export"
    - path: "packages/connector-base/src/index.ts"
      provides: "Stub placeholder export"
      contains: "export"
    - path: "packages/connector-starkbank/src/index.ts"
      provides: "Stub placeholder export"
      contains: "export"
    - path: "packages/webhooks/src/index.ts"
      provides: "Stub placeholder export"
      contains: "export"
    - path: "docs/adr/.gitkeep"
      provides: "Empty ADR directory for Phase 2 to populate"
  key_links:
    - from: "packages/core/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends: ../../tsconfig.base.json"
      pattern: "extends"
    - from: "packages/core/package.json"
      to: "packages/core/src/index.ts"
      via: "exports.development"
      pattern: "development"
    - from: "packages/core/vitest.config.ts"
      to: "vitest.config.ts (root)"
      via: "projects: glob in root config"
      pattern: "name:"
---

<objective>
Wave 1, Plan 02 — Package stubs for all 4 @aprumo/* packages. Creates the minimal per-package
structure needed for workspace resolution, typecheck, build, and test pipeline to function.

Each package gets: package.json (correct name, type:module, exports with development conditional),
tsconfig.json (composite, extends base), tsconfig.build.json (excludes tests), biome.json
(extends "//"), vitest.config.ts (name, pool:forks), src/index.ts (stub export).

Also creates docs/adr/.gitkeep so the ADR directory exists for Phase 2.

Purpose: After this plan, pnpm typecheck, pnpm build, and pnpm test all pass against stubs.
Wave 0 infra.test.ts assertions for package file existence turn GREEN.

Output:
  - 6 files per package × 4 packages = 24 files
  - docs/adr/.gitkeep
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Package structure templates from 01-RESEARCH.md Pattern 2, Pattern 4 -->

Per-package package.json shape (D-07, D-08, D-09, D-10):
  {
    "name": "@aprumo/<pkg>",
    "version": "0.0.0",
    "private": false,   // publishable (except internal packages)
    "type": "module",
    "exports": {
      ".": {
        "development": "./src/index.ts",
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      }
    },
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": ["dist"],
    "scripts": {
      "build": "tsc -p tsconfig.build.json",
      "typecheck": "tsc -p tsconfig.json --noEmit",
      "test": "vitest run",
      "lint": "biome check src/"
    }
  }

connector-base also needs subpath export for /contract (D-09):
  "exports": {
    ".": { "development": "./src/index.ts", "import": "./dist/index.js", "types": "./dist/index.d.ts" },
    "./contract": { "development": "./src/contract/index.ts", "import": "./dist/contract/index.js", "types": "./dist/contract/index.d.ts" }
  }
  (but src/contract/index.ts is a stub in Phase 1 — real implementation Phase 5)

Per-package tsconfig.json shape (Pattern 2):
  {
    "extends": "../../tsconfig.base.json",
    "compilerOptions": { "rootDir": "src", "outDir": "dist", "composite": true },
    "include": ["src/**/*.ts"],
    "references": []    // connector-starkbank will reference connector-base in Phase 5
  }

Per-package tsconfig.build.json (D-19):
  {
    "extends": "./tsconfig.json",
    "exclude": ["**/*.test.ts", "__fixtures__/**", "**/*.spec.ts"]
  }

Per-package biome.json (Pattern 3 — D-30 test override):
  {
    "extends": "//",
    "overrides": [
      {
        "include": ["**/*.test.ts"],
        "linter": { "rules": { "style": { "noNonNullAssertion": "off" } } }
      }
    ]
  }

Per-package vitest.config.ts (Pattern 4 — D-21 pool:forks):
  import { defineConfig } from "vitest/config";
  export default defineConfig({
    test: {
      name: "@aprumo/<pkg>",
      pool: "forks",
      include: ["src/**/*.test.ts"],
      environment: "node"
    }
  });

src/index.ts stub:
  // @aprumo/<pkg> — Phase 1 stub. Real implementation in Phase N.
  export {};
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install root devDependencies + create all 4 package stubs</name>
  <files>
    packages/core/package.json, packages/core/tsconfig.json, packages/core/tsconfig.build.json,
    packages/core/biome.json, packages/core/vitest.config.ts, packages/core/src/index.ts,
    packages/connector-base/package.json, packages/connector-base/tsconfig.json,
    packages/connector-base/tsconfig.build.json, packages/connector-base/biome.json,
    packages/connector-base/vitest.config.ts, packages/connector-base/src/index.ts,
    packages/connector-starkbank/package.json, packages/connector-starkbank/tsconfig.json,
    packages/connector-starkbank/tsconfig.build.json, packages/connector-starkbank/biome.json,
    packages/connector-starkbank/vitest.config.ts, packages/connector-starkbank/src/index.ts,
    packages/webhooks/package.json, packages/webhooks/tsconfig.json,
    packages/webhooks/tsconfig.build.json, packages/webhooks/biome.json,
    packages/webhooks/vitest.config.ts, packages/webhooks/src/index.ts
  </files>
  <read_first>
    - 01-RESEARCH.md §Pattern 2 (TypeScript Config — per-package shape)
    - 01-RESEARCH.md §Pattern 3 (Biome v2 — per-package extends "//" microsyntax)
    - 01-RESEARCH.md §Pattern 4 (Vitest v4 per-package config)
    - 01-RESEARCH.md §Standard Stack (exact versions for pnpm add command)
    - 01-RESEARCH.md §Anti-Patterns ("Do NOT use relative path ../../biome.json in per-package biome config")
    - 01-CONTEXT.md D-08 (ESM-only), D-09 (explicit subpath exports), D-10 (development conditional)
    - 01-CONTEXT.md D-21 (Vitest pool: 'forks')
    - 01-00-PLAN.md <interfaces> block — per-package shapes
  </read_first>
  <behavior>
    After this task:
    - Root devDependencies installed: lefthook, @biomejs/biome, vitest, @vitest/coverage-v8,
      typescript, @changesets/cli, @commitlint/cli, @commitlint/config-conventional
    - packages/core/, packages/connector-base/, packages/connector-starkbank/, packages/webhooks/
      each exist with: package.json, tsconfig.json, tsconfig.build.json, biome.json,
      vitest.config.ts, src/index.ts
    - connector-base package.json has the extra "./contract" export subpath
    - connector-base/src/contract/index.ts stub exists (export {})
    - Each vitest.config.ts has name: "@aprumo/<pkg>" and pool: "forks"
    - pnpm -r list --depth=0 shows all 4 @aprumo/* packages
    - pnpm typecheck exits 0 (stub src/index.ts files are valid TypeScript)
  </behavior>
  <action>
    STEP 1 — Install root devDependencies. Run from repo root:
    pnpm add -D -w lefthook @biomejs/biome vitest @vitest/coverage-v8 typescript @changesets/cli @commitlint/cli @commitlint/config-conventional

    Use exact versions from RESEARCH.md Standard Stack:
    - lefthook@2.1.8
    - @biomejs/biome@2.4.15
    - vitest@4.1.7
    - @vitest/coverage-v8@4.1.7
    - typescript@6.0.3
    - @changesets/cli@2.31.0
    - @commitlint/cli@21.0.1
    - @commitlint/config-conventional@21.0.1

    Full command:
    pnpm add -D -w lefthook@2.1.8 @biomejs/biome@2.4.15 vitest@4.1.7 @vitest/coverage-v8@4.1.7 typescript@6.0.3 @changesets/cli@2.31.0 @commitlint/cli@21.0.1 @commitlint/config-conventional@21.0.1

    STEP 2 — Create the 4 package directories. Use the shapes from the interfaces block above.
    For each package, create all 6 files (package.json, tsconfig.json, tsconfig.build.json,
    biome.json, vitest.config.ts, src/index.ts).

    Package names and versions (all start at 0.0.0 — Changesets will manage bumping):
    - @aprumo/core (packages/core/)
    - @aprumo/connector-base (packages/connector-base/) — extra /contract export
    - @aprumo/connector-starkbank (packages/connector-starkbank/)
    - @aprumo/webhooks (packages/webhooks/)

    For connector-base: also create packages/connector-base/src/contract/index.ts with content:
      // @aprumo/connector-base/contract — Phase 5 stub
      export {};

    The src/index.ts for each package is a single-line stub:
      export {};
    This is valid ESM TypeScript with no imports — typecheck will pass.

    After creating all files, run `pnpm install` to regenerate the lockfile with the 4 new packages
    linked. Then run `pnpm -r list --depth=0` to confirm workspace resolution.

    STEP 3 — Verify typecheck. Run `pnpm typecheck`. With stub src/index.ts files, tsc should pass
    cleanly (no errors). If tsc reports errors about missing tsconfig for referenced packages,
    check that root tsconfig.json references field points to the correct relative paths.
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && pnpm -r list --depth=0 2>&1 | grep "@aprumo" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - pnpm -r list --depth=0 shows exactly 4 @aprumo/* packages
    - Each of the 4 packages has: package.json with correct name and "type":"module"
    - Each vitest.config.ts has pool: "forks"
    - Each biome.json has "extends": "//" (NOT relative path) — grep -c '"extends": "//"' packages/*/biome.json returns 4 (one per package)
    - All 4 packages/*/biome.json files contain ""extends": "//"" (microsyntax, NOT relative paths like ../../biome.json) — verify: grep -rn 'extends.*biome' packages/*/biome.json must NOT match
    - All 4 packages/*/tsconfig.json files contain ""extends": "../../tsconfig.base.json"" — verify: grep -c 'tsconfig.base.json' packages/*/tsconfig.json returns 4
    - connector-base package.json exports has "./contract" subpath
    - connector-base/src/contract/index.ts exists
    - pnpm typecheck exits 0 (all 4 stub packages type-check clean)
    - Wave 0 tests/scaffold/infra.test.ts assertions for packages/*/src/index.ts existence turn GREEN
  </acceptance_criteria>
  <done>All 4 package stubs created; pnpm workspace resolves all @aprumo/* packages; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: docs/adr/.gitkeep + verify full pipeline (pnpm test, pnpm build, pnpm lint)</name>
  <files>docs/adr/.gitkeep</files>
  <read_first>
    - 01-CONTEXT.md §code_context — "docs/adr/ — diretório criado em Phase 1 (vazio com .gitkeep); Phase 2 começa a popular"
    - 01-RESEARCH.md §Anti-Patterns — Biome organizeImports failing silently
    - 01-RESEARCH.md §Validation Architecture §Sampling Rate
  </read_first>
  <action>
    Create docs/adr/.gitkeep (empty file, tracked by git so the directory is preserved).

    Then run the full Wave 1 verification sequence:
    1. pnpm lint — biome check on all files. Expected: 0 errors. If biome errors on the stub
       src/index.ts files, check that the files are valid TypeScript and contain no banned patterns.
    2. pnpm typecheck — tsc -b tsconfig.json --noEmit. Expected: exit 0.
    3. pnpm build — tsc -p tsconfig.build.json in each package. Expected: dist/ directories created
       with empty compiled files from stub src/index.ts.
    4. pnpm test — vitest run from root. With stub packages and no real test files yet (only the
       Wave 0 tests/scaffold/ and tests/ci/ files), vitest should run the tests we created in Plan 00.
       The tests/scaffold/infra.test.ts assertions for file existence should now pass GREEN (files exist).
       The tests/ci/ tests may still fail RED until Plan 03 (gitleaks) and Plan 05 (Changesets) complete.

    Document any failures found in this verification step in the SUMMARY. Do not block on RED tests
    in tests/ci/ — those are expected to still be RED at this wave.
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && ls docs/adr/.gitkeep && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/.gitkeep exists
    - pnpm lint exits 0 (no Biome errors on stubs)
    - pnpm typecheck exits 0
    - pnpm build exits 0 (dist/ directories created in each package)
    - pnpm test: tests/scaffold/infra.test.ts assertions for package stubs turn GREEN
    - Commit message: "feat(01-02): add package stubs for all @aprumo/* packages (INF-01)"
  </acceptance_criteria>
  <done>docs/adr/.gitkeep created; full pipeline (lint, typecheck, build, test) passes on stubs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → pnpm add | devDependencies fetched; all in Package Legitimacy Audit |
| TypeScript compiler → src/ | tsc processes stub source files; no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | pnpm add command (8 packages) | mitigate | All 8 packages passed Package Legitimacy Audit in 01-RESEARCH.md — [APPROVED]. Exact versions pinned in install command. |
| T-1-SC | Tampering | npm install supply chain | mitigate | Package Legitimacy Audit completed — no [ASSUMED]/[SUS] packages. No blocking checkpoint required. |
</threat_model>

<verification>
Plan 02 complete when:
1. pnpm -r list --depth=0 shows 4 @aprumo/* packages
2. pnpm typecheck exits 0
3. pnpm build exits 0 (dist/ created in each of 4 packages)
4. pnpm lint exits 0
5. tests/scaffold/infra.test.ts package stub assertions are GREEN
6. docs/adr/.gitkeep exists
7. Commit: "feat(01-02): add package stubs for all @aprumo/* packages (INF-01)"
</verification>

<success_criteria>
Workspace fully resolves. TypeScript, Biome, and Vitest all run cleanly against stubs. The
monorepo toolchain is functional end-to-end at the stub level. Wave 0 tests for INF-01 and
INF-04 are partially GREEN (structure exists; coverage threshold tests will verify in Plan 03+).
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-ci-dev-security/01-02-SUMMARY.md` when done
</output>
