---
phase: "01-monorepo-scaffold-ci-dev-security"
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/scaffold/infra.test.ts
  - tests/scaffold/coverage-gate-fires.test.ts
  - tests/ci/secret-scan.test.ts
  - tests/ci/gitignore.test.ts
  - tests/ci/changeset-gate.test.ts
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-03
  - INF-04
  - INF-05
  - INF-06
  - INF-07
  - INF-08
  - INF-09
  - INF-10

must_haves:
  truths:
    - "RED test for workspace resolution (INF-01) fails before package stubs exist"
    - "RED test for tsc strict compliance (INF-02) fails before tsconfig.base.json exists"
    - "RED test for Biome noExplicitAny rule (INF-03) fails before biome.json exists"
    - "pnpm vitest run --coverage exits non-zero when packages/core/src/** line coverage is below 90%"
    - "RED test for gitleaks PEM detection (INF-06) fails before gitleaks binary is available"
    - "RED test for .gitignore blocking .env* (INF-07) fails before .gitignore is created"
    - "RED test for changeset-required gate (INF-10) fails before changeset script is wired"
  decisions:
    - D-02
    - D-05
    - D-06
    - D-20
    - D-21
    - D-24
  artifacts:
    - path: "vitest.config.ts"
      provides: "Root vitest config with projects: glob and coverage thresholds"
      contains: "projects:"
    - path: "tests/scaffold/infra.test.ts"
      provides: "Failing assertions for INF-01..05 workspace/config requirements"
    - path: "tests/scaffold/coverage-gate-fires.test.ts"
      provides: "Failing assertion that vitest --coverage exits non-zero when core line coverage is below 90%"
    - path: "tests/ci/secret-scan.test.ts"
      provides: "Failing assertion that gitleaks blocks EC private key"
    - path: "tests/ci/gitignore.test.ts"
      provides: "Failing assertion that .env and *.key are git-ignored"
    - path: "tests/ci/changeset-gate.test.ts"
      provides: "Failing assertion that changeset status --since=main exits 1 with no changeset"
  key_links:
    - from: "vitest.config.ts"
      to: "packages/*/vitest.config.ts"
      via: "projects: glob"
      pattern: "projects:"
    - from: "tests/ci/secret-scan.test.ts"
      to: "gitleaks binary"
      via: "execSync('gitleaks protect --staged')"
      pattern: "gitleaks"
    - from: "tests/scaffold/coverage-gate-fires.test.ts"
      to: "packages/core/src/__fixtures__/uncovered.ts"
      via: "spawns pnpm vitest run --coverage --project=core and asserts non-zero exit"
      pattern: "coverage"
---

<objective>
Wave 0 — TDD RED scaffolds. Create the test infrastructure that enforces all 10 INF requirements.
Every test in this plan MUST fail for the correct reason after this plan completes — not because the
test is wrong, but because the implementation does not yet exist. Wave 1 turns them GREEN.

Purpose: Per CLAUDE.md "Toda mudança de código de produção começa por um teste falhando." The root
vitest.config.ts with coverage thresholds IS production infrastructure — it gates all future phases.
Creating tests first for config-presence and CI-gate scripts is how TDD is honoured even for tooling.

Output:
  - Root vitest.config.ts with projects glob and 90%/80% coverage thresholds (specific-before-wildcard)
  - tests/scaffold/infra.test.ts (RED: asserts files/fields that don't exist yet)
  - tests/scaffold/coverage-gate-fires.test.ts (RED: asserts coverage gate fires on 0%-covered fixture)
  - tests/ci/secret-scan.test.ts (RED: asserts gitleaks blocks PEM commit)
  - tests/ci/gitignore.test.ts (RED: asserts .env* and *.key are git-ignored)
  - tests/ci/changeset-gate.test.ts (RED: asserts changeset status exits 1 without changeset)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Root vitest.config.ts + Wave 0 test directory scaffold</name>
  <files>vitest.config.ts, tests/scaffold/infra.test.ts, tests/scaffold/coverage-gate-fires.test.ts</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 4 (Vitest v4 workspace + per-package coverage thresholds) — exact config shape
    - 01-RESEARCH.md §Pitfall 1 (coverage silently ignored per package) — why thresholds must be root-level
    - 01-RESEARCH.md §Pitfall 2 (vitest.workspace.ts deprecated) — use projects: in root config, NOT vitest.workspace.ts
    - 01-RESEARCH.md §Open Questions (RESOLVED) — Q3/Pattern 4: core-specific rule BEFORE wildcard
    - 01-CONTEXT.md D-20 (AMENDED 2026-05-22: vitest.config.ts raiz com array projects: — NÃO vitest.workspace.ts — depreciado desde Vitest 3.2; glob order específico-antes-de-wildcard)
    - 01-CONTEXT.md D-21 (pool: 'forks')
    - 01-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <behavior>
    RED phase assertions (tests/scaffold/infra.test.ts must fail until Wave 1 implements them):
    - pnpm-workspace.yaml exists at repo root
    - root package.json has field "packageManager" starting with "pnpm@10"
    - root package.json has engines.node = ">=22"
    - tsconfig.base.json exists at repo root
    - tsconfig.base.json contains "strict": true
    - tsconfig.base.json contains "noUncheckedIndexedAccess": true
    - tsconfig.base.json contains "module": "NodeNext"
    - biome.json exists at repo root
    - biome.json contains "noExplicitAny": "error" (nested in linter.rules)
    - .nvmrc exists at repo root
    - .npmrc exists and contains "engine-strict=true"
    - packages/core/src/index.ts exists
    - packages/connector-base/src/index.ts exists
    - packages/connector-starkbank/src/index.ts exists
    - packages/webhooks/src/index.ts exists

    RED phase assertions (tests/scaffold/coverage-gate-fires.test.ts — INF-04 gate verification):
    - Creates packages/core/src/__fixtures__/uncovered.ts with an exported function having 0% line coverage
    - Spawns `pnpm vitest run --coverage --project=@aprumo/core` as a child process
    - Asserts that the child process exits with a non-zero exit code
    - Asserts that stdout or stderr contains a coverage threshold failure marker ("ERROR: Coverage" or similar from Vitest)
    - Cleans up packages/core/src/__fixtures__/uncovered.ts in afterAll
    - This test is RED at Wave 0 because vitest.config.ts coverage thresholds cannot fire until packages/core exists; it becomes GREEN only after Wave 1 establishes the package structure
  </behavior>
  <action>
    STEP 1 — Create the root vitest.config.ts. This file is both production config and the test runner
    used to run the Wave 0 RED tests. Read 01-RESEARCH.md §Pattern 4 for the exact config shape.
    Key constraints:
    - Use defineConfig from "vitest/config" (NOT from "vite/config")
    - Use projects: ["packages/*/vitest.config.ts"] (the per-package configs are created in Plan 02)
    - Coverage provider: "v8"
    - reporters: ["text", "lcov", "json", "json-summary"]
    - reportsDirectory: "./coverage"
    - Coverage thresholds MUST be in root config (NOT per-package). Glob order: core-specific FIRST,
      wildcard SECOND. Use the exact threshold shape from RESEARCH.md §Pattern 4:
        "packages/core/src/**": { lines: 90, functions: 90, branches: 80, statements: 90 }
        "packages/*/src/**":  { lines: 80, functions: 80, branches: 75, statements: 80 }
    - Do NOT create vitest.workspace.ts — deprecated in Vitest 3.2+ (per D-20 AMENDED).

    STEP 2 — Create tests/scaffold/ directory and infra.test.ts. This test file uses Node fs and
    child_process to assert file existence and JSON/text content. It does NOT use any compiled
    package code (Wave 0 runs before packages exist).
    Import pattern: use "node:fs", "node:path", "node:child_process". No import from any @aprumo/* package.
    Each assertion is a separate it() with a descriptive name matching the behavior block above.
    Use describe("INF-01..05: Monorepo scaffold") wrapper.
    Read files with fs.existsSync() and JSON.parse(fs.readFileSync(..., 'utf8')) — safe because these
    tests run against the repo on disk.
    The test file MUST be runnable standalone with: NODE_OPTIONS=--experimental-vm-modules vitest run
    tests/scaffold/infra.test.ts

    STEP 3 — Create tests/scaffold/coverage-gate-fires.test.ts. This test verifies that the INF-04
    coverage gate actually fires (non-zero exit) when core line coverage is below 90%.
    Import pattern: use "node:fs", "node:path", "node:child_process" (spawnSync). No @aprumo/* imports.

    Structure:
    - describe("INF-04: Coverage gate fires on 0%-covered code")
    - beforeAll: create packages/core/src/__fixtures__/uncovered.ts with content:
        export function neverCalled(): string { return "I am never tested"; }
      (This function has 0% line coverage — Vitest will not see a test for it)
    - it("pnpm vitest run --coverage exits non-zero when core coverage is below 90%"): use spawnSync
      to run `pnpm vitest run --coverage --reporter=json` from repo root. Assert result.status !== 0.
      Assert that result.stdout.toString() or result.stderr.toString() contains "ERROR" or "does not meet"
      (the Vitest coverage threshold failure message).
    - afterAll: delete packages/core/src/__fixtures__/uncovered.ts if it exists (cleanup)

    IMPORTANT: This test is initially RED at Wave 0 — it fails because packages/core does not yet
    exist so the coverage run cannot fire. It becomes GREEN in Wave 1 after Plan 02 creates the
    package stubs and the coverage gate can execute against a 0%-covered fixture.

    IMPORTANT: At Wave 0 end, infra.test.ts must FAIL because pnpm-workspace.yaml, biome.json,
    tsconfig.base.json, and the package stubs do not yet exist. Run the tests and confirm they fail
    with assertion errors (not import errors or syntax errors).
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && node -e "require('./vitest.config.ts')" 2>&1 | head -5 || true; cat vitest.config.ts | grep -c "projects:"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists with projects: ["packages/*/vitest.config.ts"]
    - Coverage thresholds in vitest.config.ts: "packages/core/src/**" key appears BEFORE "packages/*/src/**"
    - tests/scaffold/infra.test.ts exists and contains at minimum 12 it() blocks
    - tests/scaffold/coverage-gate-fires.test.ts exists with beforeAll (creates fixture), it() that spawns vitest --coverage and asserts non-zero exit, and afterAll (cleanup)
    - Running the test file via vitest exits non-zero (tests are RED — pnpm-workspace.yaml does not exist yet)
    - No vitest.workspace.ts file created at repo root
    - grep -c "uncovered" tests/scaffold/coverage-gate-fires.test.ts returns >= 1
  </acceptance_criteria>
  <done>vitest.config.ts committed; tests/scaffold/infra.test.ts and coverage-gate-fires.test.ts exist and fail RED for correct reasons</done>
</task>

<task type="tdd">
  <name>Task 2: CI gate tests (secret-scan, gitignore, changeset-gate) — RED</name>
  <files>tests/ci/secret-scan.test.ts, tests/ci/gitignore.test.ts, tests/ci/changeset-gate.test.ts</files>
  <read_first>
    - 01-CONTEXT.md D-02 (gitleaks), D-05 (.gitleaks.toml rules including EC PRIVATE KEY and SECRET=)
    - 01-CONTEXT.md D-06 (two-layer: pre-commit staged + CI full history)
    - 01-RESEARCH.md §Pattern 6 (gitleaks config with private-key rule and custom Starkbank rule)
    - 01-RESEARCH.md §Pattern 8 (changeset gate: pnpm changeset status --since=main exits 1)
    - 01-RESEARCH.md §Pitfall 4 (gitleaks regression 8.7.x — version 8.30.1 required)
    - 01-RESEARCH.md §Pitfall 8 (changeset-check only on PR events, not on push to main)
    - 01-VALIDATION.md §Wave 0 Requirements (secret-scan and gitignore test stubs)
  </read_first>
  <behavior>
    tests/ci/secret-scan.test.ts RED assertions:
    - gitleaks binary is available on PATH (exits 0 when run with --version)
    - .gitleaks.toml exists at repo root
    - gitleaks protect --staged detects "-----BEGIN EC PRIVATE KEY-----" and exits non-zero
      (test creates a tmp file with the PEM header, stages it, runs gitleaks, unstages, checks exit code)
    - gitleaks protect --staged detects "SECRET=abc123supersecretvalue123456" and exits non-zero
      (same pattern with a SECRET= fixture)

    tests/ci/gitignore.test.ts RED assertions:
    - .gitignore exists at repo root
    - After `touch .env`, `git status --porcelain --ignored .env` output contains "!! .env"
    - After `touch test.key`, `git status --porcelain --ignored test.key` output contains "!! test.key"
    - After `mkdir -p secrets && touch secrets/foo`, `git status --porcelain --ignored secrets/` contains "!! secrets/"
    - After `touch dist/foo.js` (mkdir dist), `git status --porcelain --ignored dist/` contains dist

    tests/ci/changeset-gate.test.ts RED assertions:
    - scripts/changeset-required.sh exists OR changeset status command is reachable
    - `pnpm changeset status --since=main` exits 1 when no .changeset/*.md file exists for a
      publishable package change (test simulates this by checking exit code in a subprocess)
  </behavior>
  <action>
    Create tests/ci/ directory with three test files. All tests use node:child_process execSync/spawnSync
    and node:fs. No @aprumo/* imports.

    For secret-scan.test.ts:
    - Use spawnSync to call "gitleaks --version" first. If it fails, skip with a clear message:
      "gitleaks not installed — run: brew install gitleaks". This prevents the test from erroring
      cryptically on machines without gitleaks.
    - For the staged-file simulation: create a temp file in /tmp (outside the repo), use
      execSync('git -C /path/to/repo add <tmpfile>') pattern OR create a fixture at a path that
      won't be committed (use a test-only approach that does not pollute the actual staging area).
      Recommended approach: check the exit code of:
        gitleaks protect --staged --config .gitleaks.toml --redact
      against a real staged file using git's `git diff --cached --name-only` output.
      Simpler alternative: use `gitleaks detect --source /tmp/gitleaks-test-fixture/` with a
      fixture directory containing a file with `-----BEGIN EC PRIVATE KEY-----`. This avoids git
      staging complexity in tests.

    For gitignore.test.ts:
    - All touch/mkdir operations use /tmp paths or are cleaned up in afterAll.
    - Safer approach: parse .gitignore content directly and assert the required patterns are present
      (.env*, *.key, secrets/, dist/, node_modules/). This avoids git process invocation in tests.
    - Pattern check: read .gitignore with fs.readFileSync; assert each required pattern is on a
      non-comment line. Use a helper: `const patterns = content.split('\n').filter(l => !l.startsWith('#') && l.trim())`.
    - Required patterns to assert: ".env*", "*.key", "secrets/", "dist/", "node_modules/", "coverage/"

    For changeset-gate.test.ts:
    - Assert .changeset/config.json exists at repo root (this will fail RED until Plan 05 creates it)
    - Assert .changeset/config.json contains "updateInternalDependencies": "patch" per D-24/INF-09
    - Assert .changeset/config.json contains "access": "public"
    - Assert .changeset/config.json contains "baseBranch": "main"
    - Optionally assert pnpm script "changeset:check" is defined in root package.json (wired per W4)

    All three test files MUST fail RED when run now (gitleaks binary missing, .gitignore missing,
    .changeset/config.json missing). Confirm failures are assertion failures, not syntax/import errors.
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && ls tests/ci/secret-scan.test.ts tests/ci/gitignore.test.ts tests/ci/changeset-gate.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/ci/secret-scan.test.ts, tests/ci/gitignore.test.ts, tests/ci/changeset-gate.test.ts all exist
    - Each file has at least 3 it() blocks with describe() wrapper
    - No file imports from @aprumo/* packages
    - secret-scan.test.ts handles missing gitleaks binary gracefully (skip, not crash)
    - gitignore.test.ts checks for patterns: .env*, *.key, secrets/, dist/, coverage/
    - changeset-gate.test.ts checks .changeset/config.json for access, baseBranch, updateInternalDependencies
    - All three files fail RED when run (config files don't exist yet)
  </acceptance_criteria>
  <done>Three CI gate test files exist and fail RED; Wave 0 complete; ready for Wave 1 implementation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → filesystem | Tests read repo files on disk; no network, no user input |
| test runner → gitleaks binary | Tests invoke gitleaks subprocess; binary sourced from Homebrew/GitHub releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | gitleaks fixture files (-----BEGIN EC PRIVATE KEY-----) | mitigate | Use dummy fixture strings, NOT real keys. Fixture content is a PEM header only, not a valid key. All fixture files in tests/ci/ are test-only strings. |
| T-1-02 | Tampering | npm packages installed in Wave 0 (vitest, @vitest/coverage-v8) | mitigate | Versions pinned per RESEARCH.md Standard Stack; Package Legitimacy Audit passed (all packages [APPROVED]). |
| T-1-SC | Tampering | npm/pnpm installs | mitigate | Package Legitimacy Audit in 01-RESEARCH.md §Package Legitimacy Audit — all packages approved. No [ASSUMED] or [SUS] packages. |
</threat_model>

<verification>
Wave 0 is complete when:
1. vitest.config.ts exists with projects: glob and dual coverage thresholds (core 90%, wildcard 80%)
2. tests/scaffold/infra.test.ts, tests/scaffold/coverage-gate-fires.test.ts, tests/ci/secret-scan.test.ts, tests/ci/gitignore.test.ts, tests/ci/changeset-gate.test.ts all exist
3. Running any of the test files produces FAILING tests (RED), not errors/crashes
4. No vitest.workspace.ts file exists at repo root
5. coverage-gate-fires.test.ts creates fixture, spawns vitest --coverage, asserts non-zero exit, cleans up
6. Commit message: "test(01-00): add Wave 0 RED scaffolds for INF-01..10"
</verification>

<success_criteria>
All Wave 0 tests exist and fail for the correct reason (implementation does not exist yet, not
test bugs). Wave 1 and Wave 2 plans turn these tests GREEN by creating the actual config files
and package stubs.
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-ci-dev-security/01-00-SUMMARY.md` when done
</output>
