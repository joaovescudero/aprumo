---
phase: "01-monorepo-scaffold-ci-dev-security"
plan: "06"
type: execute
wave: 3
depends_on: ["01-03", "01-04", "01-05"]
files_modified:
  - README.md
  - SECURITY.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
autonomous: false
requirements:
  - INF-01
  - INF-02
  - INF-03
  - INF-04
  - INF-05
  - INF-06
  - INF-07
  - INF-08
  - INF-09
  - INF-10

user_setup:
  - service: github_branch_protection
    why: "Branch protection on main requires GitHub UI (no CLI without GitHub Apps token or admin PAT)"
    dashboard_config:
      - task: "Enable branch protection on main: Settings → Branches → Add rule → Branch name pattern: main"
      - task: "Required status checks: lint, typecheck, build, test (22), test (24), coverage-gate, changeset-check, gitleaks-history"
      - task: "Require PR before merging: enabled"
      - task: "Require 1 approving review"
      - task: "Require linear history: enabled"
      - task: "Do NOT enable signed commits yet (per D-14)"
  - service: github_pvr
    why: "GitHub Private Vulnerability Reporting must be enabled via repository Security tab"
    dashboard_config:
      - task: "Enable PVR: Settings → Security → Private vulnerability reporting → Enabled"
  - service: npm_trusted_publisher
    why: "npm OIDC trusted publisher must be configured on npm website before OIDC publish works"
    dashboard_config:
      - task: "Log in to npmjs.com → Profile → Packages → @aprumo/core → Settings → Trusted Publishing"
      - task: "Add GitHub Actions publisher: owner=<your-github-username>, repo=apruma, workflow=release.yml, environment=release"
      - task: "Repeat for @aprumo/connector-base, @aprumo/connector-starkbank, @aprumo/webhooks"
      - task: "Create GitHub environment named 'release' in repo Settings → Environments (for OIDC env lock)"

must_haves:
  truths:
    - "README.md has pre-alpha badge, project purpose, and quickstart placeholder"
    - "SECURITY.md has GitHub PVR reference and email placeholder per D-16"
    - "CONTRIBUTING.md references TDD requirement and Conventional Commits"
    - "CODE_OF_CONDUCT.md is Contributor Covenant 2.1"
    - "Human verifies branch protection and PVR are enabled after checkpoint"
  decisions:
    - D-14
    - D-15
    - D-16
    - D-23a
    - D-23b
  artifacts:
    - path: "README.md"
      provides: "Repository landing page with pre-alpha status, description, stack, quickstart"
    - path: "SECURITY.md"
      provides: "Vulnerability disclosure process with GitHub PVR and email placeholder"
      contains: "Private Vulnerability Reporting"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guide with TDD mandate, Conventional Commits, Changesets"
      contains: "TDD"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 code of conduct"
      contains: "Contributor Covenant"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "Contributing link in README"
      pattern: "CONTRIBUTING"
    - from: "SECURITY.md"
      to: "GitHub PVR"
      via: "Link to GitHub repo security tab"
      pattern: "Security Advisories"
---

<objective>
Wave 3, Plan 06 — OSS governance documents and final integration checkpoint. Creates README.md,
SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md per D-15/D-16, and pauses for human checkpoint
to configure branch protection, GitHub PVR, and npm OIDC trusted publisher (all require GitHub/npm
UI access that Claude cannot automate).

This is the last plan in Phase 1. After the human completes the checkpoint steps, the repo is
fully governance-complete: CI is live, secret scanning is active, branch protection is enforced,
and any contributor can clone → pnpm install → see CI green.

Purpose: Close out all INF requirements. D-14 (branch protection), D-15 (governance set), D-16
(PVR + SECURITY.md), D-23a (GitHub Actions OIDC — delivered Plan 04) and the deferral of D-23b
(npm Trusted Publisher — Phase 9) all land here.

Output:
  - README.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md
  - Human checkpoint: branch protection + PVR + npm trusted publisher configured
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md</name>
  <files>README.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md</files>
  <read_first>
    - 01-CONTEXT.md D-15 (governance set: what to include in each file)
    - 01-CONTEXT.md D-16 (PVR habilitado + email placeholder security@aprumo.dev)
    - 01-CONTEXT.md §Deferred — note that branding/domain is out-of-band; use placeholder email
    - CLAUDE.md §O que é o Aprumo — description to use in README
    - CLAUDE.md §Stack — stack decisions to list in README
    - CLAUDE.md §Padrão de PR / commit — Conventional Commits to reference in CONTRIBUTING
  </read_first>
  <action>
    Create README.md with:
    - Pre-alpha warning badge at the top (plain markdown badge: `![pre-alpha](https://img.shields.io/badge/status-pre--alpha-red)`)
    - H1: "Aprumo" — with one-line description from CLAUDE.md §O que é o Aprumo
    - Brief "What it is" paragraph (OSS ledger, PSP-agnostic, double-entry, Brazilian SaaS persona)
    - "What it is NOT" 2-3 bullet points (no custody, no PAN, no UI dashboard)
    - Stack section: TypeScript 6, Fastify 5, Postgres 16, pg-boss, Vitest, pnpm workspaces, Biome, Changesets
    - Quickstart placeholder section: "Full quickstart available when Phase 2 (Schema) lands — see ROADMAP"
    - Links: CONTRIBUTING.md, SECURITY.md, LICENSE
    - MIT License badge

    Create SECURITY.md per D-16:
    - H1: "Security"
    - Preferred disclosure method: GitHub Private Vulnerability Reporting (PVR) link to the repo's Security tab
    - Alternative: email security@aprumo.dev (placeholder — real email when domain exists)
    - "We follow responsible disclosure: 90-day disclosure window, CVE reserved on request"
    - "In-scope: vulnerabilities in @aprumo/* published packages"
    - "Out-of-scope: vulnerabilities in third-party connectors or self-hosted deployments"
    - Note: PAN storage is out of scope by design (SAQ-A PCI compliance)

    Create CONTRIBUTING.md per D-15:
    - H1: "Contributing to Aprumo"
    - Section "TDD is mandatory" — verbatim from CLAUDE.md §Test-Driven Development (RED-GREEN-REFACTOR)
    - Section "Conventional Commits" — types list from CLAUDE.md (feat, fix, refactor, docs, test, chore, perf, ci)
    - Section "Changesets required" — every PR that changes publishable packages needs a changeset; CI enforces
    - Section "Setup" — clone, pnpm install, pnpm test instructions
    - Section "Contract tests" — connectors must pass @aprumo/connector-base/contract suite (Phase 5+)
    - Link to CLAUDE.md for the full list of invariants and "What NOT to do" guidelines

    Create CODE_OF_CONDUCT.md with Contributor Covenant 2.1 full text. Use the canonical version
    from https://www.contributor-covenant.org/version/2/1/code_of_conduct/ — this is the standard
    short-form text. Replace [INSERT CONTACT METHOD] with: security@aprumo.dev (same placeholder).

    After creating all four files, run pnpm lint to confirm no Biome errors on markdown (Biome
    lints .md files for some patterns — ensure they pass).
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && ls README.md SECURITY.md CONTRIBUTING.md CODE_OF_CONDUCT.md && grep -c "Private Vulnerability Reporting" SECURITY.md && grep -c "TDD" CONTRIBUTING.md && grep -c "Contributor Covenant" CODE_OF_CONDUCT.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md has pre-alpha badge, Aprumo description, stack section, and CONTRIBUTING/SECURITY links
    - SECURITY.md has "Private Vulnerability Reporting" text (grep -c returns >=1) and email placeholder
    - CONTRIBUTING.md has "TDD" section (grep -c returns >=1) with RED-GREEN-REFACTOR cycle
    - CONTRIBUTING.md has "Changesets" section
    - CODE_OF_CONDUCT.md has "Contributor Covenant" text (grep -c returns >=1)
    - pnpm lint exits 0 after creating these files
    - Commit message: "docs(01-06): add OSS governance files (README, SECURITY, CONTRIBUTING, CoC) (INF-01..10)"
  </acceptance_criteria>
  <done>OSS governance documents created; all automated tasks for Phase 1 complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 6 plans (00-06) executed. The monorepo is fully scaffolded:
    - pnpm workspace with 4 @aprumo/* packages
    - TypeScript 6 + Biome + Vitest configured
    - Pre-commit hooks (gitleaks + biome + commitlint + typecheck)
    - GitHub Actions CI (7 jobs: lint, typecheck, build, test/Node22+24, coverage, changeset-check, gitleaks)
    - Changesets configured
    - OSS governance (README, SECURITY, CONTRIBUTING, CODE_OF_CONDUCT)

    Three manual steps remain that Claude cannot automate (require GitHub/npm UI):
    1. Branch protection on main (D-14)
    2. GitHub Private Vulnerability Reporting enabled (D-16)
    3. npm OIDC Trusted Publisher — D-23a (GitHub Actions side: id-token:write) was delivered in Plan 04. D-23b (npm-side Trusted Publisher configuration) is deferred to Phase 9 per CONTEXT.md D-23b.
  </what-built>
  <how-to-verify>
    Complete these 3 setup steps before approving:

    === STEP 1: GitHub Branch Protection ===
    Go to: https://github.com/<your-username>/apruma/settings/branches
    Click "Add branch protection rule" → Branch name pattern: `main`
    Enable:
      [x] Require a pull request before merging
          [x] Require 1 approving review
      [x] Require status checks to pass before merging
          Add required status checks: lint, typecheck, build, test (22), test (24), coverage-gate, gitleaks-history
          NOTE: changeset-check is optional as required check (only runs on PR, not push)
      [x] Require linear history
      [x] Include administrators
    Leave UNCHECKED: Require signed commits (per D-14 — add later when collaborators join)
    Click "Create"

    === STEP 2: GitHub Private Vulnerability Reporting ===
    Go to: https://github.com/<your-username>/apruma/security
    Click "Enable" under "Private vulnerability reporting"
    Confirm it shows "Private vulnerability reporting enabled"

    === STEP 3: npm OIDC Trusted Publisher — DEFERRED to Phase 9 (D-23b) ===
    D-23a (GitHub Actions side: id-token:write in release.yml) was delivered in Plan 04.
    D-23b (npm-side Trusted Publisher config) requires packages to already exist on npm —
    a chicken-and-egg constraint. First release uses a short-lived NPM_TOKEN; subsequent
    releases use OIDC once Trusted Publisher is configured per package on npmjs.com.
    Action required now: NONE. Revisit in Phase 9 when v0.1.0 publish is planned.

    === STEP 4: Smoke test CI ===
    Push the Phase 1 commits to GitHub. Open a PR (or push directly to verify push trigger).
    Confirm: GitHub Actions tab shows CI workflow running and jobs going green.

    After completing steps 1-2 and confirming CI is green, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" when branch protection and PVR are enabled, and CI is green on GitHub. Or type "skip-pvr" to defer PVR setup to later.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external contributor → GitHub PR | Branch protection enforces review + status checks before merge |
| security researcher → vulnerability disclosure | SECURITY.md + PVR provides safe channel |
| npm registry → package consumers | OIDC trusted publishing prevents token theft; configured post-Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | Direct push to main bypassing CI | mitigate | Branch protection (checkpoint in this plan). Required status checks include gitleaks-history so secrets scan cannot be bypassed. |
| T-1-04 | Elevation of Privilege | npm OIDC misconfiguration | accept | npm Trusted Publisher config deferred to Phase 9 (packages don't exist yet). Environment=release scope lock configured when packages are first published. Acceptable deferral — no publish risk until Phase 9. |
| T-1-01 | Information Disclosure | Starkbank ECDSA key not yet generated | accept | Key doesn't exist yet. Phase 1 secret scanning infrastructure is operational before any key is generated (the exact goal of this phase). |
</threat_model>

<verification>
Phase 1 complete when:
1. All 7 plans (00-06) executed and committed
2. pnpm test exits 0 (all Wave 0 tests GREEN or known-RED for manual-only verifications)
3. pnpm lint exits 0
4. pnpm typecheck exits 0
5. pnpm build exits 0 (dist/ in all 4 packages)
6. gitleaks protect --staged blocks EC private key
7. Branch protection enabled on main (human checkpoint)
8. GitHub Actions CI runs green on pushed commits
9. ROADMAP Phase 1 success criteria 1-5 all TRUE
</verification>

<success_criteria>
Phase 1 complete. Operators can clone the repo, run pnpm install, and see the pipeline pass in CI
(Node 22, Node 24). Pre-commit secret scanning blocks credential commits before the first Starkbank
key is ever generated. All 10 INF requirements satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-ci-dev-security/01-06-SUMMARY.md` when done
</output>
