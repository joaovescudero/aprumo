---
phase: "01-monorepo-scaffold-ci-dev-security"
plan: "05"
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - .changeset/config.json
  - scripts/changeset-required.sh
autonomous: true
requirements:
  - INF-09
  - INF-10

must_haves:
  truths:
    - ".changeset/config.json has access:public, updateInternalDependencies:patch, no linked, baseBranch:main"
    - "pnpm changeset exits 0 and produces a valid .changeset/*.md file interactively"
    - "pnpm changeset status --since=main exits 1 when no changeset file exists for publishable package"
    - "Wave 0 tests/ci/changeset-gate.test.ts assertions pass GREEN"
  artifacts:
    - path: ".changeset/config.json"
      provides: "Changesets configuration per D-24"
      contains: "updateInternalDependencies"
    - path: "scripts/changeset-required.sh"
      provides: "CI gate script asserting pnpm changeset status exits 1 without changeset (for local testing)"
  key_links:
    - from: ".changeset/config.json"
      to: ".github/workflows/ci.yml changeset-check job"
      via: "pnpm changeset status --since=main reads baseBranch from config.json"
      pattern: "baseBranch"
    - from: ".changeset/config.json"
      to: ".github/workflows/release.yml"
      via: "changesets/action@v1 reads config.json for package access and internal deps"
      pattern: "access"
---

<objective>
Wave 3, Plan 05 — Changesets configuration. Creates .changeset/config.json and the CI gate script
that the changeset-check job uses. After this plan, INF-09 and INF-10 are fully implemented.

Purpose: ROADMAP success criterion #5 — "Changesets is configured; pnpm changeset works and
produces a valid changeset file." The Wave 0 tests/ci/changeset-gate.test.ts assertions for
.changeset/config.json fields will turn GREEN.

Output:
  - .changeset/config.json (access:public, updateInternalDependencies:patch, baseBranch:main)
  - scripts/changeset-required.sh (for local CI gate simulation)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold-ci-dev-security/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Pattern 8: Changesets config from 01-RESEARCH.md -->

.changeset/config.json shape (D-24, INF-09):
  {
    "$schema": "https://unpkg.com/@changesets/config/schema.json",
    "changelog": "@changesets/cli/changelog",
    "commit": false,
    "fixed": [],
    "linked": [],
    "access": "public",
    "baseBranch": "main",
    "updateInternalDependencies": "patch",
    "ignore": [],
    "privatePackages": { "version": true, "tag": false }
  }

CI gate command (Pattern 8, Pitfall 8):
  pnpm changeset status --since=main
  Exits 1 if no changeset present for a publishable package change
  ONLY run on pull_request events (already gated in ci.yml from Plan 04)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: .changeset/config.json + verify changeset gate</name>
  <files>.changeset/config.json, scripts/changeset-required.sh, package.json</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 8 (Changesets Configuration — full config.json content and CI gate command)
    - 01-RESEARCH.md §Pitfall 8 (Changeset Status on Non-PR Branch — gate only on PR events)
    - 01-CONTEXT.md D-22 (changesets/action behavior), D-23 (OIDC publish), D-24 (.changeset/config.json fields)
    - REQUIREMENTS.md INF-09 (updateInternalDependencies: patch, no linked, independent versioning per package)
    - REQUIREMENTS.md INF-10 (CI blocks PR without changeset when publishable package changes)
  </read_first>
  <behavior>
    After this task:
    - .changeset/config.json exists with all required fields per D-24:
        access: "public", baseBranch: "main", updateInternalDependencies: "patch"
        linked: [] (empty — independent versioning per INF-09), commit: false
    - pnpm changeset (run interactively) detects the 4 @aprumo/* packages as publishable
    - pnpm changeset status --since=main exits 1 (no changeset file exists yet — correct behavior)
    - tests/ci/changeset-gate.test.ts assertions pass GREEN (config.json has required fields)
    - root package.json has "changeset:check" script wired to scripts/changeset-required.sh
  </behavior>
  <action>
    STEP 1 — Create .changeset/ directory and config.json. Use the exact shape from RESEARCH.md
    §Pattern 8. Key fields that must match INF-09/INF-10:
    - "linked": [] — independent per-package versioning (NOT linked groups)
    - "updateInternalDependencies": "patch" — when package A depends on package B and B bumps,
      A gets a patch bump automatically
    - "access": "public" — all @aprumo/* packages are published as public npm packages
    - "commit": false — changesets does NOT auto-commit version bumps (developer controls commits)
    - "privatePackages": { "version": true, "tag": false } — root package is private but can
      still have version bumped (avoids Changesets error on the root)

    Do NOT add any packages to "ignore" in Phase 1 — all 4 are publishable from Phase 1.

    STEP 2 — Create scripts/changeset-required.sh. This is a helper script for local testing of
    the CI gate. Content:
      #!/usr/bin/env bash
      # Simulate the CI changeset-check gate locally.
      # Usage: bash scripts/changeset-required.sh
      # Exits 1 if no changeset file exists for a publishable package change since main.
      # Note: In CI, this runs only on pull_request events (see .github/workflows/ci.yml).
      set -e
      pnpm changeset status --since=main

    Make it executable: chmod +x scripts/changeset-required.sh

    STEP 2b — Wire scripts/changeset-required.sh as the root package.json script "changeset:check".
    This ensures local and CI use a single source of truth:
    - In root package.json scripts section, add: "changeset:check": "bash scripts/changeset-required.sh"
    - The CI changeset-check job in ci.yml calls undefined
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "changeset:check" not found (or undefined
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "changeset" not found
      directly — both work; using the npm script adds a local-equivalent entry point).
    - After adding the script, run undefined
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "changeset:check" not found to verify it works and exits as expected.

    STEP 3 — Run `pnpm changeset status --since=main` from repo root. Since no changeset file
    exists yet, this should exit 1. If it exits 0, check: is the current branch already main?
    On main branch with no pending changesets, the command may exit 0. This is correct — the
    command is designed for PR branches. On a feature branch, it would exit 1.

    STEP 4 — Run pnpm test to confirm Wave 0 changeset-gate tests pass GREEN:
    The tests/ci/changeset-gate.test.ts assertions for .changeset/config.json existence and
    field values should now pass.
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && node -e "const c=JSON.parse(require('fs').readFileSync('.changeset/config.json','utf8')); console.log(c.access, c.updateInternalDependencies, c.baseBranch, c.linked.length)"</automated>
  </verify>
  <acceptance_criteria>
    - .changeset/config.json exists and is valid JSON
    - .changeset/config.json "access" is "public"
    - .changeset/config.json "updateInternalDependencies" is "patch"
    - .changeset/config.json "baseBranch" is "main"
    - .changeset/config.json "linked" is [] (empty array)
    - .changeset/config.json "commit" is false
    - scripts/changeset-required.sh exists and is executable (ls -la shows -rwxr-xr-x)
    - root package.json has "changeset:check" script: node -e "const p=require('./package.json'); console.log(p.scripts['changeset:check'])" outputs the bash command
    - undefined
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "changeset:check" not found is runnable (single source of truth between local and CI)
    - tests/ci/changeset-gate.test.ts assertions for config.json pass GREEN
    - Commit message: "feat(01-05): add Changesets config for independent package versioning (INF-09, INF-10)"
  </acceptance_criteria>
  <done>Changesets configured; INF-09 and INF-10 fully implemented; changeset-gate tests GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → pnpm changeset → npm registry | OIDC trusted publishing prevents long-lived tokens; real publish only in Phase 9 |
| CI → changeset status check | Guards against publishable changes without version documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Elevation of Privilege | Premature npm publish via changesets/action | mitigate | release.yml publish step only fires when "Version Packages" PR is merged to main. In Phase 1, no packages have been published — no risk of accidental publish. Real publish deferred to Phase 9 per D-22. |
| T-1-02 | Tampering | @changesets/cli@2.31.0 (installed in Plan 02) | mitigate | Package in Legitimacy Audit — [APPROVED]. Already installed. |
</threat_model>

<verification>
Plan 05 complete when:
1. .changeset/config.json has access:"public", updateInternalDependencies:"patch", baseBranch:"main"
2. .changeset/config.json has linked:[] (empty)
3. scripts/changeset-required.sh is executable
4. tests/ci/changeset-gate.test.ts passes GREEN
5. pnpm changeset status exits expected code (1 on PR branch without changesets, 0 on main)
</verification>

<success_criteria>
Changesets fully configured. INF-09 (independent versioning) and INF-10 (CI changeset gate)
are both satisfied. The complete Wave 3 combination (Plans 03+04+05) delivers ROADMAP success
criteria #2 through #5.
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-ci-dev-security/01-05-SUMMARY.md` when done
</output>
