---
phase: 03-core-ledger-api
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/core/src/lib/with-retry.ts
  - packages/core/src/lib/with-retry.test.ts
  - packages/core/src/api/errors/app-errors.ts
  - packages/core/src/api/errors/pg-error-handler.ts
  - packages/core/src/api/errors/pg-error-handler.test.ts
autonomous: true
requirements:
  - API-10
  - API-11

must_haves:
  truths:
    - "withRetryOnSerializationFailure retries the full factory fn on SQLSTATE 40001 up to 3 times then re-throws"
    - "withRetryOnSerializationFailure succeeds on the second attempt when first throws 40001"
    - "pgErrorHandler maps PG P0001 containing double_entry_violation to HTTP 422 problem+json"
    - "pgErrorHandler maps exhausted SQLSTATE 40001 to HTTP 503 with retryable:true"
    - "pgErrorHandler maps Fastify validation errors to HTTP 422 problem+json"
    - "pgErrorHandler never leaks internal SQL error details or stack traces"
    - "Error responses use RFC 9457 application/problem+json format per D-02"
  artifacts:
    - path: "packages/core/src/lib/with-retry.ts"
      provides: "withRetryOnSerializationFailure<T>(fn: () => Promise<T>): Promise<T>"
      exports: ["withRetryOnSerializationFailure"]
    - path: "packages/core/src/lib/with-retry.test.ts"
      provides: "Unit tests: retry succeeds on 2nd attempt; throws after 3 retries; non-40001 errors are not retried"
    - path: "packages/core/src/api/errors/app-errors.ts"
      provides: "LedgerError, ValidationError, NotFoundError custom Error subclasses with code/statusCode"
      exports: ["LedgerError", "ValidationError", "NotFoundError", "SerializationRetryExhaustedError"]
    - path: "packages/core/src/api/errors/pg-error-handler.ts"
      provides: "pgErrorHandler: FastifyErrorHandler — maps PG codes to RFC 9457 problem+json"
      exports: ["pgErrorHandler"]
    - path: "packages/core/src/api/errors/pg-error-handler.test.ts"
      provides: "Unit tests: P0001 → 422; 40001 → 503; FST_ERR_VALIDATION → 422; unknown → 500 without stack"
  key_links:
    - from: "packages/core/src/lib/with-retry.ts"
      to: "packages/core/src/api/routes/transactions.ts"
      via: "import in route handler wrapping db.transaction()"
      pattern: "withRetryOnSerializationFailure"
    - from: "packages/core/src/api/errors/pg-error-handler.ts"
      to: "packages/core/src/api/server.ts"
      via: "app.setErrorHandler(pgErrorHandler)"
      pattern: "setErrorHandler"
---

<objective>
TDD plan for the two pure-unit modules: withRetryOnSerializationFailure and pgErrorHandler + app-errors. No DB connection needed — all tests use vi.fn() mock factories.

Purpose: These modules are the most critical correctness guarantees in Phase 3. withRetry must wrap the entire db.transaction() factory (not just the failing SQL). The error handler must map PG codes to RFC 9457 problem+json without leaking internals.
Output: with-retry.ts + with-retry.test.ts (GREEN); app-errors.ts; pg-error-handler.ts + pg-error-handler.test.ts (GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-core-ledger-api/03-CONTEXT.md
@.planning/phases/03-core-ledger-api/03-RESEARCH.md
@.planning/phases/03-core-ledger-api/03-PATTERNS.md
@packages/core/tests/helpers/applyMigrationsToSchema.ts
@packages/core/src/db/schema.ts
</context>

<interfaces>
<!-- Pattern for PG error code extraction (from applyMigrationsToSchema.ts ~line 376): -->
<!-- const pgCode = err instanceof Error && 'code' in err ? (err as { code: string }).code : undefined; -->
<!-- For FastifyError cast: (error as unknown as { code?: string }).code -->

<!-- Fastify error handler type signature: -->
<!-- type FastifyErrorHandler = (error: FastifyError, request: FastifyRequest, reply: FastifyReply) => void -->
<!-- FastifyError has: message, validation (array if validation error), statusCode, code -->

<!-- RFC 9457 problem+json structure (D-02): -->
<!-- { type: string (URI), title: string, status: number, detail: string, code: string (machine-readable), instance: string (req correlation id) } -->
<!-- Content-Type: application/problem+json -->
<!-- instance is set to request.id (correlation id per D-09) -->

<!-- Error code machine-readable strings (D-02 specifics): -->
<!-- unbalanced_postings, idempotency_key_required, serialization_retry_exhausted, validation_error, not_found, internal_error -->
</interfaces>

<feature>
  <name>withRetryOnSerializationFailure + pgErrorHandler</name>
  <files>
    packages/core/src/lib/with-retry.ts,
    packages/core/src/lib/with-retry.test.ts,
    packages/core/src/api/errors/app-errors.ts,
    packages/core/src/api/errors/pg-error-handler.ts,
    packages/core/src/api/errors/pg-error-handler.test.ts
  </files>
  <behavior>
    withRetryOnSerializationFailure:
    - Input: factory fn `() => Promise<T>` that throws { code: '40001' } on first call, resolves 'ok' on second → output: resolves 'ok'
    - Input: factory fn that throws { code: '40001' } on all 4 calls → output: throws (re-throws last error after 3 retries)
    - Input: factory fn that throws { code: '23505' } (not 40001) → output: throws immediately without retry
    - Input: factory fn that resolves immediately → output: resolves with value; fn called exactly once
    - Backoff: delays between retries are 50ms, 100ms, 200ms (use vi.useFakeTimers to assert timing without real waits)

    pgErrorHandler:
    - Input: error with code='P0001' and message containing 'do not balance' → output: reply.status(422), Content-Type: application/problem+json, body.code='unbalanced_postings'
    - Input: error with code='P0001' and message containing other P0001 text → output: reply.status(422), body.code='ledger_constraint_violation'
    - Input: error with code='40001' → output: reply.status(503), body.code='serialization_retry_exhausted', body.retryable=true
    - Input: Fastify validation error (has .validation array) → output: reply.status(422), body.code='validation_error', body.detail includes validation errors
    - Input: error with code='23505' (unique_violation) → output: reply.status(409), body.code='conflict'
    - Input: error with unknown code → output: reply.status(500), body.code='internal_error', body.detail does NOT contain any SQL keywords, error.message, or stack
    - All responses: Content-Type header set to 'application/problem+json'
    - All responses: body.instance set to request.id (correlation id)
    - Missing Idempotency-Key (400): handler receives error from route validation with code='idempotency_key_required' → reply.status(400)
  </behavior>
  <implementation>
    RED cycle:
    1. Create with-retry.test.ts and pg-error-handler.test.ts with all failing assertions (import non-existent modules). Run `pnpm --filter @aprumo/core test -- with-retry` and `pnpm --filter @aprumo/core test -- pg-error-handler` — both must fail with import errors or assertion failures.

    GREEN cycle for with-retry.ts:
    - Implement withRetryOnSerializationFailure<T>(fn: () => Promise<T>): Promise<T>
    - SERIALIZATION_FAILURE constant = '40001'
    - MAX_RETRIES constant = 3
    - Loop: attempt 0..MAX_RETRIES. On catch: if code === '40001' AND attempt < MAX_RETRIES, store lastError, await setTimeout(50 * 2**attempt), continue. Else throw.
    - After loop: throw lastError (unreachable in correct flow, satisfies TypeScript)
    - Extract pgCode using the established project pattern: `err instanceof Error && 'code' in err ? (err as { code: string }).code : undefined`
    - File: packages/core/src/lib/with-retry.ts (kebab-case, .ts extension, .js on imports per ESM)

    GREEN cycle for app-errors.ts:
    - LedgerError extends Error with readonly code: string and readonly statusCode: number
    - SerializationRetryExhaustedError extends LedgerError (code='serialization_retry_exhausted', statusCode=503, retryable=true)
    - ValidationError extends LedgerError (code='validation_error', statusCode=422)
    - NotFoundError extends LedgerError (code='not_found', statusCode=404)
    - Set this.name = 'LedgerError' (etc.) in each constructor per CLAUDE.md convention

    GREEN cycle for pg-error-handler.ts:
    - Import FastifyError, FastifyReply, FastifyRequest types from 'fastify'
    - pgErrorHandler function with signature: (error: FastifyError & { code?: string; validation?: unknown[] }, req: FastifyRequest, reply: FastifyReply) => void
    - Build problem+json helper: buildProblem(status, code, title, detail, instance) → plain object
    - Set reply.header('content-type', 'application/problem+json') on every branch
    - Discrimination order: 1) validation array present → 422, 2) pgCode === 'P0001' → discriminate on message prefix, 3) pgCode === '40001' → 503, 4) pgCode === '23505' → 409, 5) default → 500
    - For 500: never include error.message or error.stack in the response body. detail = 'An unexpected error occurred.'
    - instance = req.id (Fastify assigns genReqId on each request per D-09)
    - For missing Idempotency-Key: the route schema will handle this as a validation error (body.validation array) — the handler maps to 400 when the missing field is 'idempotency-key' header

    REFACTOR: ensure no `any` types; use `unknown` + type guards throughout.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD RED — write failing tests for withRetryOnSerializationFailure</name>
  <read_first>
    packages/core/tests/helpers/applyMigrationsToSchema.ts (lines 232-257: readWithRetry loop pattern; lines 370-390: pgCode extraction idiom)
    packages/core/tests/helpers/createTestDb.test.ts (unit test file structure without DB)
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Pattern 4: withRetryOnSerializationFailure)
    .planning/phases/03-core-ledger-api/03-PATTERNS.md (Unit test files section)
  </read_first>
  <behavior>
    - fn resolves 'ok' after throwing 40001 on first call → withRetry resolves 'ok'
    - fn throws 40001 on calls 1-4 (beyond MAX_RETRIES=3) → withRetry re-throws
    - fn throws 23505 on first call → withRetry re-throws immediately (no retry)
    - fn resolves immediately → withRetry resolves; fn called exactly once (vi.fn() call count assertion)
    - vi.useFakeTimers() used to avoid real 50-200ms waits in tests
  </behavior>
  <action>
    Create packages/core/src/lib/with-retry.test.ts. Import withRetryOnSerializationFailure from './with-retry.js' (file does not exist yet — this causes import error = RED state). Write describe/it blocks for all 4 behaviors above using vi.fn() mock factories. Use vi.useFakeTimers() in beforeEach and vi.useRealTimers() in afterEach. For the timing test, advance fake timers with vi.advanceTimersByTimeAsync() between retry attempts. Run: `pnpm --filter @aprumo/core test -- with-retry 2>&1 | head -20` — must show FAIL or import error.
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- with-retry 2>&1 | grep -E 'FAIL|Cannot find|Error' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - with-retry.test.ts exists with at least 4 test cases covering the behaviors above
    - Running the test command exits non-zero (tests are RED — module not yet implemented)
    - No syntax errors in the test file (only import-resolution failures)
  </acceptance_criteria>
  <done>with-retry.test.ts written; test command exits non-zero (RED state confirmed)</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD RED — write failing tests for pgErrorHandler + app-errors</name>
  <read_first>
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Pattern 5: Error Handler + error code constants)
    .planning/phases/03-core-ledger-api/03-CONTEXT.md (D-02: RFC 9457; D-03: instance=correlation id; D-09: X-Request-Id)
    packages/core/tests/helpers/createTestDb.test.ts (unit test structure)
  </read_first>
  <behavior>
    - error { code: 'P0001', message: 'postings do not balance' } → reply.status(422), body.code='unbalanced_postings', Content-Type='application/problem+json'
    - error { code: '40001' } → reply.status(503), body.code='serialization_retry_exhausted', body.retryable=true
    - error { validation: [{ message: 'must be string' }] } → reply.status(422), body.code='validation_error'
    - error { code: '23505' } → reply.status(409), body.code='conflict'
    - error { code: undefined, message: 'SELECT * FROM secrets WHERE...' } → reply.status(500), body.detail does NOT contain 'SELECT', body.detail does NOT contain 'secrets'
    - All responses: body.instance equals req.id mock value
  </behavior>
  <action>
    Create packages/core/src/api/errors/pg-error-handler.test.ts. Import pgErrorHandler from './pg-error-handler.js' and app-errors from './app-errors.js' (both non-existent = RED). Mock the FastifyReply with vi.fn() stubs for status(), send(), and header() returning the mock reply for chaining. Mock FastifyRequest with id: 'test-req-id'. Write describe/it blocks for all 6 behaviors above. Run: `pnpm --filter @aprumo/core test -- pg-error-handler 2>&1 | head -20` — must show FAIL.
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- pg-error-handler 2>&1 | grep -E 'FAIL|Cannot find|Error' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - pg-error-handler.test.ts exists with at least 6 test cases
    - Test command exits non-zero (RED state)
    - Reply mock has status(), send(), header() vi.fn() stubs
  </acceptance_criteria>
  <done>pg-error-handler.test.ts written; test command exits non-zero (RED)</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: TDD GREEN — implement with-retry.ts, app-errors.ts, pg-error-handler.ts</name>
  <read_first>
    packages/core/src/lib/with-retry.test.ts (just written — tests define the contract)
    packages/core/src/api/errors/pg-error-handler.test.ts (just written)
    packages/core/tests/helpers/applyMigrationsToSchema.ts (lines 232-257 for retry loop; lines 370-390 for pgCode extraction)
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Pattern 4 and Pattern 5 code templates)
    .planning/phases/03-core-ledger-api/03-CONTEXT.md (D-02, D-03, D-09 for problem+json contract)
  </read_first>
  <action>
    Implement three files to turn tests GREEN:

    1. packages/core/src/lib/with-retry.ts:
       - Export: withRetryOnSerializationFailure<T>(fn: () => Promise<T>): Promise<T>
       - SERIALIZATION_FAILURE = '40001', MAX_RETRIES = 3
       - Loop i from 0 to MAX_RETRIES inclusive. Try fn(). On catch: extract pgCode from err using `err instanceof Error && 'code' in err ? (err as { code: string }).code : undefined`. If code === '40001' and i < MAX_RETRIES: store lastError, await setTimeout(50 * 2**i)ms, continue. Else: throw err immediately.
       - After loop exits normally: unreachable, but TypeScript requires throw lastError at end.
       - Strict TypeScript: no any; use type guard for code extraction.
       - File uses ESM: all relative imports use .js extension.

    2. packages/core/src/api/errors/app-errors.ts:
       - Create directory packages/core/src/api/errors/ if it does not exist.
       - Export LedgerError extends Error: constructor(message: string, public readonly code: string, public readonly statusCode: number); set this.name = 'LedgerError'.
       - Export SerializationRetryExhaustedError extends LedgerError: constructor(); calls super('Serialization retry exhausted', 'serialization_retry_exhausted', 503); this.name = 'SerializationRetryExhaustedError'.
       - Export ValidationError extends LedgerError, NotFoundError extends LedgerError (statusCodes 422, 404).
       - No any. No throw string.

    3. packages/core/src/api/errors/pg-error-handler.ts:
       - Import types from 'fastify': FastifyError, FastifyReply, FastifyRequest.
       - buildProblem(status: number, code: string, title: string, detail: string, instance: string): record type for application/problem+json body. Include type (URN like 'urn:aprumo:error:<code>'), title, status, detail, code, instance.
       - Export pgErrorHandler(error: FastifyError & { code?: string; validation?: unknown[] }, req: FastifyRequest, reply: FastifyReply): void.
       - Set header 'content-type' = 'application/problem+json' on every branch before send.
       - Discrimination (per behavior tests): validation array → 422 code='validation_error'; P0001 with 'do not balance' → 422 code='unbalanced_postings'; P0001 other → 422 code='ledger_constraint_violation'; 40001 → 503 code='serialization_retry_exhausted' with retryable:true appended to body; 23505 → 409 code='conflict'; default → 500 code='internal_error' detail='An unexpected error occurred.' (never leak error.message).
       - instance = req.id.
       - pgCode extraction: (error as unknown as { code?: string }).code (per PATTERNS.md analog).
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- with-retry pg-error-handler 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @aprumo/core test -- with-retry` exits 0 with all tests green
    - `pnpm --filter @aprumo/core test -- pg-error-handler` exits 0 with all tests green
    - `pnpm --filter @aprumo/core typecheck` exits 0
    - No `any` in with-retry.ts, app-errors.ts, or pg-error-handler.ts (confirm with: `grep -rn '\bany\b' packages/core/src/lib/with-retry.ts packages/core/src/api/errors/`)
    - pg-error-handler.ts 500 path: `grep -c 'error.message\|error.stack' packages/core/src/api/errors/pg-error-handler.ts` outputs 0 (no leakage)
  </acceptance_criteria>
  <done>All tests GREEN; typecheck passes; no any; 500 path does not leak internals</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PG driver error → HTTP response | pgErrorHandler must not echo PG error details to client |
| Client → retry wrapper | Serialization retry logic must not exceed 3 attempts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03a | Information Disclosure | pgErrorHandler 500 path | mitigate | detail field set to 'An unexpected error occurred.' — never error.message or error.stack; asserted by unit test |
| T-03-03b | Denial of Service | withRetryOnSerializationFailure | mitigate | MAX_RETRIES=3 hard-coded; exponential backoff 50/100/200ms; 4th failure re-throws immediately preventing infinite retry |
| T-03-03c | Tampering | error code discrimination | mitigate | pgCode extracted via type-safe `instanceof Error && 'code' in err` guard — never cast or assume error shape; unknown errors fall through to 500 |
</threat_model>

<verification>
Full verification after GREEN cycle:
- `pnpm --filter @aprumo/core test -- with-retry pg-error-handler` exits 0
- `pnpm --filter @aprumo/core typecheck` exits 0
- `grep -rn '\bany\b' packages/core/src/lib/with-retry.ts packages/core/src/api/errors/` outputs nothing
- `grep -c 'error\.message\|\.stack' packages/core/src/api/errors/pg-error-handler.ts` outputs 0
</verification>

<success_criteria>
withRetryOnSerializationFailure: retries full factory on 40001 up to 3x; throws on 4th; non-40001 errors propagate immediately.
pgErrorHandler: P0001 → 422, 40001 → 503 retryable, validation → 422, 500 never leaks internals.
All unit tests GREEN. Typecheck passes. No any.
</success_criteria>

<output>
Create `.planning/phases/03-core-ledger-api/03-03-SUMMARY.md` when done
</output>
