---
phase: 03-core-ledger-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
autonomous: true
requirements:
  - API-01
  - API-02
  - API-13
user_setup: []

must_haves:
  truths:
    - "fastify, @fastify/swagger, @fastify/swagger-ui, @fastify/type-provider-typebox, @sinclair/typebox are present in packages/core/package.json dependencies"
    - "pnpm install completes without error after adding the new packages"
    - "pnpm --filter @aprumo/core typecheck exits 0 after install (no new TS errors from missing types)"
  artifacts:
    - path: "packages/core/package.json"
      provides: "Updated dependency list with Fastify + TypeBox ecosystem"
      contains: "fastify"
  key_links:
    - from: "packages/core/package.json"
      to: "packages/core/src/api/server.ts"
      via: "pnpm install makes fastify importable"
      pattern: "\"fastify\""
---

<objective>
Install the Fastify 5 ecosystem packages required for the Core Ledger API into @aprumo/core.

Purpose: All Phase 3 plans depend on these packages being resolvable. This plan gates all subsequent work — no route, schema, plugin, or server factory can be authored until these imports resolve.

Output: Updated packages/core/package.json with fastify + TypeBox ecosystem; pnpm lockfile updated; typecheck still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-ledger-api/03-RESEARCH.md
@packages/core/package.json
</context>

<interfaces>
<!-- Packages to install — all [ASSUMED] from Package Legitimacy Audit in 03-RESEARCH.md -->
<!-- All are official Fastify organization packages on npm; verified via npm view during research -->

New production dependencies:
  fastify@5.8.5
  @fastify/swagger@9.7.0
  @fastify/swagger-ui@5.2.6
  @fastify/type-provider-typebox@6.1.0
  @sinclair/typebox@0.34.49

New dev dependencies:
  pino-pretty@13.1.3

Already present (no change needed):
  drizzle-orm@^0.45.2
  postgres@^3.4.9
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-core-ledger-api/03-RESEARCH.md (Package Legitimacy Audit section — all packages tagged [ASSUMED])
  </read_first>
  <what-built>
    All six packages are pre-audited as [ASSUMED] in 03-RESEARCH.md (slopcheck unavailable at research time). All are official Fastify organization packages with 3+ years on npm, 500K+ weekly downloads each. Confirm these are the packages you expect before install proceeds.

    Packages:
    - fastify@5.8.5 — github.com/fastify/fastify (core framework, ADR-002 locked)
    - @fastify/swagger@9.7.0 — github.com/fastify/fastify-swagger (official plugin)
    - @fastify/swagger-ui@5.2.6 — github.com/fastify/fastify-swagger-ui (official plugin)
    - @fastify/type-provider-typebox@6.1.0 — github.com/fastify/fastify-type-provider-typebox (official plugin)
    - @sinclair/typebox@0.34.49 — github.com/sinclairzx81/typebox (>10M/wk downloads)
    - pino-pretty@13.1.3 (devDep) — github.com/pinojs/pino-pretty (official pino ecosystem)

    Verify: open each package URL on npmjs.com if desired, confirm publisher is "fastify" or "sinclairzx81" or "pinojs".
  </what-built>
  <how-to-verify>
    For each package, optionally run: npm view fastify | head -5, npm view @fastify/swagger | head -5, etc.
    Confirm the "maintainers" field shows the official org, not an unknown publisher.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe any concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Fastify ecosystem packages into @aprumo/core</name>
  <files>packages/core/package.json</files>
  <read_first>
    - packages/core/package.json (current dependencies — drizzle-orm and postgres already present)
    - .planning/phases/03-core-ledger-api/03-RESEARCH.md (Standard Stack section, exact versions verified)
    - CLAUDE.md (no `any`, strict TS, kebab-case files, Biome not ESLint)
  </read_first>
  <action>
    Run the install command from 03-RESEARCH.md Standard Stack section, scoped to @aprumo/core:

    pnpm --filter @aprumo/core add fastify@5.8.5 "@fastify/swagger@9.7.0" "@fastify/swagger-ui@5.2.6" "@fastify/type-provider-typebox@6.1.0" "@sinclair/typebox@0.34.49"

    Then add pino-pretty as a devDependency:

    pnpm --filter @aprumo/core add -D pino-pretty@13.1.3

    Do NOT pin exact versions in package.json with `=` — use the caret `^` that pnpm defaults to. Do NOT add fastify types manually (`@types/fastify` does not exist; fastify ships its own types).

    After install, run typecheck to confirm no new TS errors:

    pnpm --filter @aprumo/core typecheck

    If typecheck fails due to missing DOM types or node types, do NOT add `@types/node` (already a transitive dep). Investigate the specific error first.
  </action>
  <verify>
    <automated>cd /Users/jvescudero/projects/apruma && pnpm --filter @aprumo/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/package.json contains "fastify", "@fastify/swagger", "@fastify/swagger-ui", "@fastify/type-provider-typebox", "@sinclair/typebox" under "dependencies"
    - packages/core/package.json contains "pino-pretty" under "devDependencies"
    - `pnpm --filter @aprumo/core typecheck` exits 0 (no new TS errors introduced by the install)
    - pnpm-lock.yaml updated (no manual edits needed — pnpm handles this)
  </acceptance_criteria>
  <done>All six packages resolvable in @aprumo/core; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package download from registry; supply chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm install (fastify ecosystem) | mitigate | Human-verify checkpoint before install; all packages are [ASSUMED] from official Fastify org — verified by human in Task 1 |
| T-03-SC | Tampering | npm/pnpm installs | mitigate | slopcheck unavailable at research time; Task 1 checkpoint is blocking-human for [ASSUMED] packages; human verifies publisher identity on npmjs.com before Task 2 proceeds |
</threat_model>

<verification>
pnpm --filter @aprumo/core typecheck
grep -c "fastify" packages/core/package.json
</verification>

<success_criteria>
- packages/core/package.json has fastify + TypeBox ecosystem in dependencies
- pino-pretty in devDependencies
- pnpm --filter @aprumo/core typecheck exits 0
- No pre-existing tests broken (pnpm --filter @aprumo/core test still passes)
</success_criteria>

<output>
Create `.planning/phases/03-core-ledger-api/03-01-SUMMARY.md` when done.
</output>
