---
phase: 03-core-ledger-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
autonomous: false
requirements:
  - API-01
  - API-13
user_setup: []

must_haves:
  truths:
    - "fastify, @fastify/swagger, @fastify/swagger-ui, @fastify/type-provider-typebox, @sinclair/typebox are installed in packages/core"
    - "pino-pretty is a devDependency in packages/core"
    - "pnpm install completes without error after package additions"
  artifacts:
    - path: "packages/core/package.json"
      provides: "Updated dependency list with fastify ecosystem"
      contains: "fastify"
  key_links:
    - from: "packages/core/package.json"
      to: "packages/core/src/api/server.ts"
      via: "runtime dependency resolution"
      pattern: "\"fastify\""
---

<objective>
Install the Fastify 5 ecosystem packages into @aprumo/core. This is a pre-requisite for all API implementation plans (Plans 03-03 through 03-07).

Purpose: Wave 1 package install so downstream plans can import fastify, TypeBox, and swagger plugins without resolution errors.
Output: Updated packages/core/package.json with fastify ecosystem as runtime deps and pino-pretty as devDep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-ledger-api/03-CONTEXT.md
@.planning/phases/03-core-ledger-api/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify package legitimacy before install</name>
  <read_first>
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Package Legitimacy Audit section — all 6 packages are [ASSUMED])
  </read_first>
  <what-built>
    All 6 new packages are [ASSUMED] in the Package Legitimacy Audit (slopcheck was unavailable during research). Packages:
    - fastify@5.8.5 (github.com/fastify/fastify — official framework)
    - @fastify/swagger@9.7.0 (github.com/fastify/fastify-swagger — official plugin)
    - @fastify/swagger-ui@5.2.6 (github.com/fastify/fastify-swagger-ui — official plugin)
    - @fastify/type-provider-typebox@6.1.0 (github.com/fastify/fastify-type-provider-typebox — official plugin)
    - @sinclair/typebox@0.34.49 (github.com/sinclairzx81/typebox — widely used)
    - pino-pretty@13.1.3 (github.com/pinojs/pino-pretty — official pino ecosystem)
  </what-built>
  <how-to-verify>
    For each package, verify on npmjs.com that:
    1. Package name matches exactly (no typos, no lookalike names)
    2. Publisher matches the expected GitHub org (fastify, sinclairzx81, pinojs)
    3. Weekly downloads are in the millions (not suspiciously low)
    4. Last publish date is recent (not abandoned years ago)

    Quick checks:
    - https://www.npmjs.com/package/fastify
    - https://www.npmjs.com/package/@fastify/swagger
    - https://www.npmjs.com/package/@fastify/swagger-ui
    - https://www.npmjs.com/package/@fastify/type-provider-typebox
    - https://www.npmjs.com/package/@sinclair/typebox
    - https://www.npmjs.com/package/pino-pretty
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe any package that looks suspicious</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install fastify ecosystem packages</name>
  <read_first>
    packages/core/package.json (current deps — drizzle-orm and postgres are already present; do NOT duplicate them)
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Standard Stack section for exact versions)
  </read_first>
  <action>
    Run the following pnpm commands from the repo root to add packages to @aprumo/core only:

    Runtime dependencies (add to `dependencies`):
      pnpm --filter @aprumo/core add fastify@5.8.5 @fastify/swagger@9.7.0 @fastify/swagger-ui@5.2.6 @fastify/type-provider-typebox@6.1.0 @sinclair/typebox@0.34.49

    Dev dependency (add to `devDependencies`):
      pnpm --filter @aprumo/core add -D pino-pretty@13.1.3

    Do NOT add drizzle-orm or postgres — they are already present.
    Do NOT add pino — it is bundled with Fastify 5 and does not need an explicit dep.

    After install, verify packages/core/package.json contains all 6 new entries.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('packages/core/package.json','utf8')); const deps = {...p.dependencies,...p.devDependencies}; const required=['fastify','@fastify/swagger','@fastify/swagger-ui','@fastify/type-provider-typebox','@sinclair/typebox','pino-pretty']; const missing=required.filter(k=>!deps[k]); if(missing.length){process.stderr.write('MISSING: '+missing.join(', ')+'\n');process.exit(1);}else{console.log('All packages present');}"</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/package.json has `"fastify": "^5.8.5"` (or >=5.8.5) in dependencies
    - packages/core/package.json has `"@fastify/swagger": "^9.7.0"` in dependencies
    - packages/core/package.json has `"@fastify/swagger-ui": "^5.2.6"` in dependencies
    - packages/core/package.json has `"@fastify/type-provider-typebox": "^6.1.0"` in dependencies
    - packages/core/package.json has `"@sinclair/typebox": "^0.34.49"` in dependencies
    - packages/core/package.json has `"pino-pretty": "^13.1.3"` in devDependencies
    - `pnpm install` exits 0 (lock file updated without conflict)
    - `node -e "require('./packages/core/node_modules/fastify')"` exits 0 from repo root (module resolves)
  </acceptance_criteria>
  <done>All 6 packages installed; packages/core/package.json updated; pnpm-lock.yaml updated; pnpm install exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | 6 new packages pulled from npm; all tagged [ASSUMED] in legitimacy audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | npm package install | mitigate | Human checkpoint (Task 1) verifies all 6 packages on npmjs.com before install runs |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable; blocking human-verify checkpoint before any install |
</threat_model>

<verification>
After both tasks complete:
- `node -e "const p=JSON.parse(require('fs').readFileSync('packages/core/package.json','utf8')); ['fastify','@fastify/swagger','@fastify/swagger-ui','@fastify/type-provider-typebox','@sinclair/typebox'].forEach(k=>{ if(!p.dependencies[k]) throw new Error('missing '+k); }); if(!p.devDependencies['pino-pretty']) throw new Error('missing pino-pretty'); console.log('ok');"` exits 0
- `pnpm typecheck` from repo root still exits 0 (no TS errors from the new packages)
</verification>

<success_criteria>
All 6 packages installed and resolvable in packages/core. pnpm-lock.yaml committed. No TS errors introduced.
</success_criteria>

<output>
Create `.planning/phases/03-core-ledger-api/03-01-SUMMARY.md` when done
</output>
