---
phase: 03-core-ledger-api
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/core/src/api/schemas/transaction.ts
  - packages/core/src/api/schemas/account.ts
  - packages/core/src/api/schemas/common.ts
  - packages/core/src/api/plugins/bigint-serializer.ts
  - packages/core/src/api/server.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-13

must_haves:
  truths:
    - "createServer(db) factory returns a Fastify instance with TypeBox type provider, coerceTypes:false, BigInt serializer, swagger, and error handler registered"
    - "BigInt values in HTTP responses are serialized as JSON strings (not numbers), verified by a server-level integration test"
    - "TypeBox schemas for transactions and accounts define amount_cents as Type.String() in responses and Type.Integer({ minimum:1 }) in requests"
    - "GET /docs returns 200 (swagger UI registered before routes)"
    - "OpenAPI spec at GET /docs/json documents amount_cents as type:string"
    - "Fastify configured with coerceTypes:false, removeAdditional:all, onProtoPoisoning:error"
  artifacts:
    - path: "packages/core/src/api/server.ts"
      provides: "createServer(db) factory — injectable for tests and main.ts"
      exports: ["createServer", "AnyDrizzleDb"]
    - path: "packages/core/src/api/plugins/bigint-serializer.ts"
      provides: "registerBigIntSerializer(app) — setSerializerCompiler with JSON.stringify replacer"
      exports: ["registerBigIntSerializer"]
    - path: "packages/core/src/api/schemas/transaction.ts"
      provides: "TypeBox schemas for POST /v1/transactions and GET /v1/transactions/:id"
      exports: ["PostTransactionBodySchema", "PostTransactionHeadersSchema", "TransactionResponseSchema", "PostingResponseSchema", "GetTransactionParamsSchema"]
    - path: "packages/core/src/api/schemas/account.ts"
      provides: "TypeBox schemas for POST/GET /v1/accounts and GET /v1/accounts/:id/postings"
      exports: ["PostAccountBodySchema", "AccountResponseSchema", "PostingsPageResponseSchema", "GetAccountParamsSchema", "GetPostingsQuerySchema"]
    - path: "packages/core/src/api/schemas/common.ts"
      provides: "ProblemSchema (RFC 9457), PaginationCursorSchema, shared types"
      exports: ["ProblemSchema"]
  key_links:
    - from: "packages/core/src/api/server.ts"
      to: "packages/core/src/api/errors/pg-error-handler.ts"
      via: "app.setErrorHandler(pgErrorHandler)"
      pattern: "setErrorHandler"
    - from: "packages/core/src/api/server.ts"
      to: "packages/core/src/api/plugins/bigint-serializer.ts"
      via: "registerBigIntSerializer(app) called before route registration"
      pattern: "registerBigIntSerializer"
    - from: "packages/core/src/api/schemas/transaction.ts"
      to: "packages/core/src/api/routes/transactions.ts"
      via: "TypeBox schema imports for route handler type inference"
      pattern: "PostTransactionBodySchema"
---

<objective>
Build the TypeBox schemas, BigInt serializer plugin, and Fastify server factory. These are the shared infrastructure consumed by all route plans (Plans 03-05, 03-06, 03-07). No routes are registered here — the server factory exposes a prefix-plugin interface for routes to register themselves.

Purpose: Interface-first ordering — route plans import schemas/types from this plan's artifacts. Executors of Plans 03-05 and 03-06 get all TypeBox type inference for free.
Output: server.ts (createServer factory), bigint-serializer.ts plugin, 3 schema files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-core-ledger-api/03-CONTEXT.md
@.planning/phases/03-core-ledger-api/03-RESEARCH.md
@.planning/phases/03-core-ledger-api/03-PATTERNS.md
@packages/core/src/db/schema.ts
@packages/core/src/api/errors/pg-error-handler.ts
</context>

<interfaces>
<!-- Drizzle instance types needed for server factory: -->
<!-- Production: drizzle-orm/postgres-js → PostgresJsDatabase -->
<!-- Tests: drizzle-orm/node-postgres → NodePgDatabase -->
<!-- Solution: type AnyDrizzleDb = PostgresJsDatabase | NodePgDatabase (export from server.ts) -->

<!-- Fastify 5 TypeBox provider import: -->
<!-- import Fastify from 'fastify'; -->
<!-- import { TypeBoxTypeProvider } from '@fastify/type-provider-typebox'; -->
<!-- const app = Fastify({...}).withTypeProvider<TypeBoxTypeProvider>(); -->

<!-- swagger must be registered BEFORE routes (Pitfall 4 in RESEARCH.md) -->

<!-- BigInt serializer (RESEARCH.md Pattern 1 + Code Example): -->
<!-- app.setSerializerCompiler(() => (data) => JSON.stringify(data, (_k, v) => typeof v === 'bigint' ? v.toString() : v)) -->

<!-- TypeBox schema field conventions (PATTERNS.md + RESEARCH.md): -->
<!-- amount_cents in REQUEST body: Type.Integer({ minimum: 1, description: 'Amount in cents' }) -->
<!-- amount_cents in RESPONSE: Type.String({ description: 'Amount in cents, BigInt serialized as decimal string' }) -->
<!-- UUIDs: Type.String({ format: 'uuid' }) -->
<!-- timestamps: Type.String({ format: 'date-time' }) -->
<!-- idempotency-key header (note: HTTP headers are lowercased by Fastify): Type.String({ minLength: 1, maxLength: 255 }) -->

<!-- Cursor pagination response envelope (D-05): -->
<!-- { data: T[], next_cursor: string | null } -->
<!-- D-06: default limit=50, hard max=200, >200 → 400 problem+json -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeBox schema files (transaction.ts, account.ts, common.ts)</name>
  <read_first>
    packages/core/src/db/schema.ts (column types: all UUID ids, bigint amount_cents, text direction, text type, jsonb metadata, timestamp fields — authoritative source for schema alignment)
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (TypeBox Schema for POST /transactions code example; Pattern 7 pagination)
    .planning/phases/03-core-ledger-api/03-CONTEXT.md (D-01: /v1 prefix; D-04: cursor by created_at DESC; D-05: opaque base64 cursor; D-06: default 50, max 200; D-11: snake_case fields; D-12: UUID strings)
  </read_first>
  <action>
    Create directory packages/core/src/api/schemas/ if it does not exist.

    1. packages/core/src/api/schemas/common.ts:
       - Import Type from '@sinclair/typebox'
       - Export ProblemSchema: Type.Object({ type: Type.String(), title: Type.String(), status: Type.Integer(), detail: Type.String(), code: Type.String(), instance: Type.String() }) — RFC 9457 per D-02
       - Export CursorPageSchema<T>(itemSchema: T): Type.Object({ data: Type.Array(T), next_cursor: Type.Union([Type.String(), Type.Null()]) }) using Type.Object with generic item type

    2. packages/core/src/api/schemas/transaction.ts:
       - Import Type from '@sinclair/typebox'
       - PostingInputSchema: { account_id: Type.String({format:'uuid'}), amount_cents: Type.Integer({minimum:1, description:'Amount in cents'}), direction: Type.Union([Type.Literal('debit'), Type.Literal('credit')]) }
       - PostTransactionBodySchema: { postings: Type.Array(PostingInputSchema, {minItems:2}), description: Type.Optional(Type.String({maxLength:500})), metadata: Type.Optional(Type.Object({},{additionalProperties:true})) }
       - PostTransactionHeadersSchema: Type.Object({ 'idempotency-key': Type.String({minLength:1, maxLength:255}) }) — lowercase header name (Fastify lowercases headers)
       - PostingResponseSchema: { id: uuid string, transaction_id: uuid string, account_id: uuid string, amount_cents: Type.String({description:'BigInt as string'}), direction: debit|credit union, created_at: Type.String({format:'date-time'}) }
       - TransactionResponseSchema: { id: uuid string, idempotency_key: Type.String(), ts: Type.String({format:'date-time'}), description: Type.Optional(Type.String()), source: Type.Optional(Type.String()), metadata: Type.Optional(Type.Object({},{additionalProperties:true})), postings: Type.Array(PostingResponseSchema) }
       - GetTransactionParamsSchema: Type.Object({ id: Type.String({format:'uuid'}) })
       - Export all the above

    3. packages/core/src/api/schemas/account.ts:
       - Import Type from '@sinclair/typebox'
       - PostAccountBodySchema: { type: Type.Union([Type.Literal('asset'), Type.Literal('liability'), Type.Literal('revenue'), Type.Literal('expense'), Type.Literal('equity')]), owner_ref: Type.String({minLength:1}), metadata: Type.Optional(Type.Object({},{additionalProperties:true})) }
       - AccountResponseSchema: { id: uuid string, type: string union (same 5 literals), owner_ref: Type.String(), metadata: Type.Optional(Type.Object({},{additionalProperties:true})), created_at: Type.String({format:'date-time'}), balance: Type.Union([Type.String({description:'BigInt as string'}), Type.Null()]) } — balance is null when no account_balance row yet (API-08)
       - GetAccountParamsSchema: Type.Object({ id: Type.String({format:'uuid'}) })
       - GetPostingsParamsSchema: Type.Object({ id: Type.String({format:'uuid'}) })
       - GetPostingsQuerySchema: Type.Object({ cursor: Type.Optional(Type.String()), limit: Type.Optional(Type.Integer({minimum:1, maximum:200, default:50})) }) — D-06: max 200; validate >200 → 400 (Fastify Ajv does this automatically with maximum:200)
       - PostingsPageResponseSchema: Type.Object({ data: Type.Array(PostingResponseSchema imported from transaction.ts), next_cursor: Type.Union([Type.String(), Type.Null()]) })
       - Export all

    All schema files: no any; ESM .js extensions on relative imports; kebab-case filenames already correct.
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 3 schema files exist in packages/core/src/api/schemas/
    - `pnpm --filter @aprumo/core typecheck` exits 0
    - PostingResponseSchema has amount_cents as Type.String() (not Type.Integer()) — confirmed by: `grep -n 'amount_cents' packages/core/src/api/schemas/transaction.ts | grep 'Type.String'`
    - PostingInputSchema has amount_cents as Type.Integer() — confirmed by: `grep -n 'amount_cents' packages/core/src/api/schemas/transaction.ts | grep 'Type.Integer'`
    - PostTransactionHeadersSchema uses lowercase 'idempotency-key' key name
  </acceptance_criteria>
  <done>3 schema files written; typecheck passes; amount_cents is String in response schemas and Integer in request schemas</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bigint-serializer plugin and createServer factory</name>
  <read_first>
    packages/core/src/api/schemas/transaction.ts (just created — import PostingResponseSchema to confirm amount_cents is Type.String())
    packages/core/src/api/errors/pg-error-handler.ts (pgErrorHandler to wire into setErrorHandler)
    .planning/phases/03-core-ledger-api/03-RESEARCH.md (Pattern 1: BigInt serializer; Pattern 2: Server factory; Pitfall 3: coerceTypes; Pitfall 4: swagger before routes; Code Examples section)
    .planning/phases/03-core-ledger-api/03-CONTEXT.md (D-07: single /health; D-09: genReqId + X-Request-Id; D-10: pino redact)
  </read_first>
  <behavior>
    - createServer(db) called with mock db → returns Fastify app with status 200 on GET /docs
    - createServer(db) called → BigInt value in reply.send({ amount: 100n }) is serialized to '"100"' in JSON (string, not number)
    - Fastify instance has coerceTypes:false (send number for string field → 422)
    - GET /docs/json returns OpenAPI spec with amount_cents field documented as type:string
  </behavior>
  <action>
    Create directory packages/core/src/api/plugins/ if it does not exist.

    1. packages/core/src/api/plugins/bigint-serializer.ts:
       - Import FastifyInstance from 'fastify'
       - Export registerBigIntSerializer(app: FastifyInstance): void
       - Call app.setSerializerCompiler(() => (data) => JSON.stringify(data, (_key, value) => typeof value === 'bigint' ? value.toString() : value))
       - No other logic; single-responsibility

    2. packages/core/src/api/server.ts:
       - Import Fastify from 'fastify'; TypeBoxTypeProvider from '@fastify/type-provider-typebox'; swagger from '@fastify/swagger'; swaggerUi from '@fastify/swagger-ui'
       - Import pgErrorHandler from './errors/pg-error-handler.js'
       - Import registerBigIntSerializer from './plugins/bigint-serializer.js'
       - Export type AnyDrizzleDb (accept either postgres.js or node-postgres drizzle instance). Use: import type { PostgresJsDatabase } from 'drizzle-orm/postgres-js'; import type { NodePgDatabase } from 'drizzle-orm/node-postgres'; export type AnyDrizzleDb = PostgresJsDatabase<Record<string,unknown>> | NodePgDatabase<Record<string,unknown>>
       - Export async function createServer(db: AnyDrizzleDb): Promise<FastifyInstance>
       - Fastify constructor options:
         * logger: { level: process.env['NODE_ENV'] === 'test' ? 'silent' : 'info', redact: ['req.headers.authorization', 'req.headers.cookie', 'req.headers["idempotency-key"]'] } — per D-10: Idempotency-Key header value is redacted; never log request/response bodies
         * genReqId: () => crypto.randomUUID() — per D-09 (import { randomUUID } from 'node:crypto')
         * onProtoPoisoning: 'error' — per RESEARCH.md security patterns
         * ajv: { customOptions: { coerceTypes: false, removeAdditional: 'all', useDefaults: true } }
       - Call .withTypeProvider<TypeBoxTypeProvider>() on the Fastify instance
       - Register BigInt serializer: registerBigIntSerializer(app) — MUST be before any plugin or route registration
       - Register swagger BEFORE routes (Pitfall 4): await app.register(swagger, { openapi: { openapi: '3.0.0', info: { title: 'Aprumo Ledger API', version: '0.1.0', description: 'Immutable double-entry ledger API. amount_cents fields are JSON strings (BigInt precision).' } } })
       - Register swagger-ui: await app.register(swaggerUi, { routePrefix: '/docs' })
       - Wire error handler: app.setErrorHandler(pgErrorHandler)
       - Add X-Request-Id response hook: app.addHook('onSend', (_req, reply, _payload, done) => { reply.header('x-request-id', _req.id); done(); })
       - Route plugins are NOT registered here — they will be registered in Plans 03-05, 03-06, 03-07.
       - Return app
       - File stores db parameter so route plugins can receive it — export type ServerOptions for route plugin option types: { db: AnyDrizzleDb }

    3. Write a minimal server.test.ts to verify BigInt serialization and swagger registration (TDD-lite — not a full TDD plan but asserts the two highest-risk behaviors):
       - File: packages/core/src/api/server.test.ts
       - Import createTestDb (createTestDb returns pg.Pool; create drizzle instance from it for the test)
       - Test: register a one-off route that returns { amount_cents: 100n } → assert response body parsed as JSON has typeof amount_cents === 'string' and value === '100'
       - Test: GET /docs returns 200
       - Test: GET /docs/json returns 200 and body contains '"amount_cents"' with '"type":"string"'
       - Use app.inject() — no port listening
       - beforeAll: createTestDb + createServer; afterAll: app.close() + db.cleanup()
  </action>
  <verify>
    <automated>pnpm --filter @aprumo/core test -- server.test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/api/server.ts exists and exports createServer and AnyDrizzleDb
    - packages/core/src/api/plugins/bigint-serializer.ts exists and exports registerBigIntSerializer
    - `pnpm --filter @aprumo/core test -- server.test` exits 0 with all tests green
    - `pnpm --filter @aprumo/core typecheck` exits 0
    - server.test.ts BigInt test: response body amount_cents is a JSON string '100', not number 100
    - server.test.ts swagger test: GET /docs returns 200
    - Fastify constructor has onProtoPoisoning: 'error' — confirmed by: `grep -c 'onProtoPoisoning' packages/core/src/api/server.ts` outputs at least 1
    - Fastify constructor has coerceTypes: false — confirmed by: `grep -c 'coerceTypes' packages/core/src/api/server.ts` outputs at least 1
    - Idempotency-Key header value is in the redact array in logger config
  </acceptance_criteria>
  <done>server.ts and bigint-serializer.ts written; server.test.ts GREEN; typecheck passes; BigInt serialized as string confirmed by test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → Fastify route handler | coerceTypes:false + removeAdditional:all prevent type coercion attacks and mass assignment |
| JSON response → API consumer | setSerializerCompiler ensures BigInt never reaches native JSON.stringify (which throws) |
| Request logs → pino output | redact config prevents Idempotency-Key and auth headers from appearing in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04a | Tampering | Ajv coerceTypes | mitigate | coerceTypes: false in Fastify constructor; numeric idempotency_key cannot be silently coerced to string |
| T-03-04b | Tampering | Mass assignment | mitigate | removeAdditional: 'all' strips unknown fields from request bodies before handlers see them |
| T-03-04c | Tampering | Prototype pollution | mitigate | onProtoPoisoning: 'error' in Fastify constructor rejects __proto__ injection attempts |
| T-03-04d | Information Disclosure | pino request logs | mitigate | redact array covers authorization, cookie, and idempotency-key header values; request/response bodies never logged wholesale |
| T-03-04e | Information Disclosure | OpenAPI spec at /docs | accept | /docs is unauthenticated (auth deferred per CONTEXT.md); spec contains no secrets, only schema documentation |
</threat_model>

<verification>
After both tasks complete:
- `pnpm --filter @aprumo/core test -- server.test` exits 0
- `pnpm --filter @aprumo/core typecheck` exits 0
- `grep -c 'coerceTypes: false' packages/core/src/api/server.ts` outputs 1
- `grep -c 'onProtoPoisoning' packages/core/src/api/server.ts` outputs 1
- `grep -c 'idempotency-key' packages/core/src/api/server.ts` outputs at least 1 (in redact)
- server.test.ts response for { amount_cents: 100n } has typeof === 'string'
</verification>

<success_criteria>
createServer factory ready for route registration. BigInt → string serialization verified by test. Swagger registered before routes. coerceTypes:false. No PII or auth headers in logs. Typecheck passes.
</success_criteria>

<output>
Create `.planning/phases/03-core-ledger-api/03-04-SUMMARY.md` when done
</output>
